<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:mcp="http://www.mulesoft.org/schema/mule/mcp"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/mcp http://www.mulesoft.org/schema/mule/mcp/current/mule-mcp.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd">

    <!--
        MCP tool-listener flows for the Platform Lifecycle Orchestrator.
        These flows expose the same business logic as the REST API endpoints
        as MCP tools — consumable by AI agents (Claude, GitHub Copilot, etc.)
        via the Streamable HTTP transport configured in global-config.xml.

        Each flow:
          1. Receives parameters from the MCP client as payload
          2. Maps parameters to variables
          3. Delegates to a shared private sub-flow (pf-*) that also serves the REST API
          4. Returns the sub-flow result as the MCP tool response

        Shared private sub-flows are defined at the bottom of this file.
        REST entry points call the same sub-flows from:
          src/main/mule/implementation/post-jobs-assess.xml
          src/main/mule/implementation/post-jobs.xml
          src/main/mule/implementation/get-job-status.xml
    -->

    <!-- ===== Tool 1: assess_app ===== -->
    <flow name="assess_app-mcp-tool"
          doc:id="mc1a0001-0001-0001-0001-000000000001"
          doc:name="MCP Tool: assess_app"
          doc:description="MCP tool entry point for assessing a MuleSoft application's Java 17 upgrade readiness">

        <mcp:tool-listener
            config-ref="MCP_Config"
            name="assess_app"
            doc:name="MCP Tool Listener: assess_app"
            doc:id="mc1a0001-0001-0001-0001-000000000002">
            <mcp:description>
                Assess a deployed MuleSoft application for Java 17 / Mule 4.9.x upgrade readiness.
                Returns the current runtime version, recommended change plan, and any warnings that
                require human review before submitting an upgrade job. Call this before start_upgrade.
            </mcp:description>
            <mcp:parameters-schema><![CDATA[{
  "type": "object",
  "required": ["appName", "environment"],
  "properties": {
    "appName": {
      "type": "string",
      "description": "Name of the MuleSoft application as it appears in Anypoint Runtime Manager."
    },
    "environment": {
      "type": "string",
      "description": "Anypoint Platform environment where the app is running (e.g. Production, Staging, Development)."
    },
    "jiraTicketId": {
      "type": "string",
      "description": "Optional Jira ticket ID to link assessment results back to a migration tracking ticket."
    }
  }
}]]></mcp:parameters-schema>
            <mcp:responses>
                <mcp:text-tool-response-content text="#[output application/json --- payload]" />
            </mcp:responses>
        </mcp:tool-listener>

        <!-- Snapshot inbound payload FIRST so vars.requestBody is the tool-parameters object,
             not a later-overwritten value (e.g. after pf-load-matrix clobbers payload). -->
        <set-variable variableName="requestBody"
                      value="#[payload]"
                      doc:name="PV SET requestBody"
                      doc:id="mc1a0001-0001-0001-0001-000000000007" />
        <!-- Map MCP tool parameters to variables consumed by the shared sub-flow -->
        <set-variable variableName="appName"
                      value="#[payload.appName]"
                      doc:name="PV SET appName"
                      doc:id="mc1a0001-0001-0001-0001-000000000003" />
        <set-variable variableName="environment"
                      value="#[payload.environment]"
                      doc:name="PV SET environment"
                      doc:id="mc1a0001-0001-0001-0001-000000000004" />
        <set-variable variableName="jiraTicketId"
                      value="#[payload.jiraTicketId default null]"
                      doc:name="PV SET jiraTicketId"
                      doc:id="mc1a0001-0001-0001-0001-000000000005" />

        <flow-ref name="pf-assess-app"
                  doc:name="pf-assess-app"
                  doc:id="mc1a0001-0001-0001-0001-000000000006" />

    </flow>

    <!-- ===== Tool 2: start_upgrade ===== -->
    <flow name="start_upgrade-mcp-tool"
          doc:id="mc2b0002-0002-0002-0002-000000000001"
          doc:name="MCP Tool: start_upgrade"
          doc:description="MCP tool entry point for submitting an approved Java 17 upgrade job">

        <mcp:tool-listener
            config-ref="MCP_Config"
            name="start_upgrade"
            doc:name="MCP Tool Listener: start_upgrade"
            doc:id="mc2b0002-0002-0002-0002-000000000002">
            <mcp:description>
                Start an asynchronous Java 17 upgrade job for a MuleSoft application after the user
                has reviewed and approved the change plan from assess_app. Returns a jobId that can
                be polled with get_job_status to track progress (PROCESSING → COMMITTED → PR_OPEN → DEPLOYED).
            </mcp:description>
            <mcp:parameters-schema><![CDATA[{
  "type": "object",
  "required": ["appName", "environment", "jiraTicketId", "approvedChangePlan"],
  "properties": {
    "appName": {
      "type": "string",
      "description": "Name of the MuleSoft application to upgrade."
    },
    "environment": {
      "type": "string",
      "description": "Anypoint Platform environment to upgrade the application in."
    },
    "jiraTicketId": {
      "type": "string",
      "description": "Jira ticket ID for this migration effort. Progress updates will be posted to this ticket."
    },
    "approvedChangePlan": {
      "type": "object",
      "description": "The changePlan object returned by assess_app, reviewed and approved by the user.",
      "properties": {
        "targetRuntime":     { "type": "string" },
        "targetJavaVersion": { "type": "string" },
        "filesToChange":     { "type": "array", "items": { "type": "string" } },
        "hasApiPolicies":    { "type": "boolean" },
        "hasCustomJavaCode": { "type": "boolean" },
        "hasLookupFunction": { "type": "boolean" }
      }
    }
  }
}]]></mcp:parameters-schema>
            <mcp:responses>
                <mcp:text-tool-response-content text="#[output application/json --- payload]" />
            </mcp:responses>
        </mcp:tool-listener>

        <!-- Snapshot inbound payload FIRST so vars.requestBody is the tool-parameters object. -->
        <set-variable variableName="requestBody"
                      value="#[payload]"
                      doc:name="PV SET requestBody"
                      doc:id="mc2b0002-0002-0002-0002-000000000008" />
        <!-- Map MCP tool parameters to variables consumed by the shared sub-flow -->
        <set-variable variableName="appName"
                      value="#[payload.appName]"
                      doc:name="PV SET appName"
                      doc:id="mc2b0002-0002-0002-0002-000000000003" />
        <set-variable variableName="environment"
                      value="#[payload.environment]"
                      doc:name="PV SET environment"
                      doc:id="mc2b0002-0002-0002-0002-000000000004" />
        <set-variable variableName="jiraTicketId"
                      value="#[payload.jiraTicketId]"
                      doc:name="PV SET jiraTicketId"
                      doc:id="mc2b0002-0002-0002-0002-000000000005" />
        <set-variable variableName="approvedChangePlan"
                      value="#[payload.approvedChangePlan]"
                      doc:name="PV SET approvedChangePlan"
                      doc:id="mc2b0002-0002-0002-0002-000000000006" />

        <flow-ref name="pf-start-upgrade"
                  doc:name="pf-start-upgrade"
                  doc:id="mc2b0002-0002-0002-0002-000000000007" />

    </flow>

    <!-- ===== Tool 3: get_job_status ===== -->
    <flow name="get_job_status-mcp-tool"
          doc:id="mc3c0003-0003-0003-0003-000000000001"
          doc:name="MCP Tool: get_job_status"
          doc:description="MCP tool entry point for retrieving the current status of an upgrade job">

        <mcp:tool-listener
            config-ref="MCP_Config"
            name="get_job_status"
            doc:name="MCP Tool Listener: get_job_status"
            doc:id="mc3c0003-0003-0003-0003-000000000002">
            <mcp:description>
                Retrieve the current status of a platform migration job submitted by start_upgrade.
                Poll this tool until status reaches a terminal state: DEPLOYED (success),
                FAILED (error — inspect the error field), or COMMITTED (changes on branch, awaiting PR merge).
                When status is PR_OPEN the prUrl field contains the GitHub pull request link.
            </mcp:description>
            <mcp:parameters-schema><![CDATA[{
  "type": "object",
  "required": ["jobId"],
  "properties": {
    "jobId": {
      "type": "string",
      "description": "The jobId returned by start_upgrade. Format: job-{uuid}."
    }
  }
}]]></mcp:parameters-schema>
            <mcp:responses>
                <mcp:text-tool-response-content text="#[output application/json --- payload]" />
            </mcp:responses>
        </mcp:tool-listener>

        <!-- Map MCP tool parameters to variables consumed by the shared sub-flow -->
        <set-variable variableName="jobId"
                      value="#[payload.jobId]"
                      doc:name="PV SET jobId"
                      doc:id="mc3c0003-0003-0003-0003-000000000003" />

        <flow-ref name="pf-get-job-status"
                  doc:name="pf-get-job-status"
                  doc:id="mc3c0003-0003-0003-0003-000000000004" />

    </flow>

    <!-- ==========================================================================
         Shared Private Sub-flows
         Called by BOTH MCP tool-listener flows (above) AND REST implementation
         flows in src/main/mule/implementation/. Variables must be set by the
         caller before invoking via flow-ref.
         ========================================================================== -->

    <!-- pf-assess-app: assesses application migration readiness             -->
    <!-- Requires: vars.appName, vars.environment, vars.jiraTicketId(opt)    -->
    <!-- Returns:  payload = AssessmentResult JSON                           -->
    <sub-flow name="pf-assess-app"
              doc:id="pf000001-0001-0001-0001-000000000001"
              doc:name="pf-assess-app"
              doc:description="Shared: assess MuleSoft app for Java 17 upgrade. Called by REST and MCP.">

        <logger level="DEBUG"
                doc:name="LOG DEBUG pf-assess-app enter"
                doc:id="pf000001-0001-0001-0001-000000000002"
                message="#['LOG DEBUG | flow=pf-assess-app | correlationId=' ++ correlationId ++ ' | app=' ++ (vars.appName default 'unknown') ++ ' | env=' ++ (vars.environment default 'unknown')]" />

        <!-- ── Step 1: Load matrix (→ vars.matrix) and resolve coordinates (→ vars.coords) ── -->
        <flow-ref name="pf-load-matrix"
                  doc:name="pf-load-matrix"
                  doc:id="pf000001-0001-0001-0001-000000000010" />
        <flow-ref name="pf-resolve-coordinates"
                  doc:name="pf-resolve-coordinates"
                  doc:id="pf000001-0001-0001-0001-000000000011" />

        <!-- ── Step 2: Input validation ── -->
        <choice doc:name="CHOICE env empty?"
                doc:id="pf000001-0001-0001-0001-000000000012"
                doc:description="Reject immediately if no environment was supplied">
            <when expression="#[vars.environment == null or vars.environment == '']"
                  doc:name="environment is empty"
                  doc:id="pf000001-0001-0001-0001-000000000013">
                <raise-error type="VALIDATION:ENV_REQUIRED"
                             description="vars.environment must be set before calling pf-assess-app"
                             doc:name="RAISE VALIDATION:ENV_REQUIRED"
                             doc:id="pf000001-0001-0001-0001-000000000014" />
            </when>
            <otherwise doc:name="environment present"
                       doc:id="pf000001-0001-0001-0001-000000000015">
                <logger level="DEBUG"
                        doc:name="LOG DEBUG env present"
                        doc:id="pf000001-0001-0001-0001-000000000016"
                        message="#['LOG DEBUG | flow=pf-assess-app | env=' ++ vars.environment ++ ' | owner=' ++ (vars.coords.owner default '') ++ ' | repo=' ++ (vars.coords.repo default '')]" />
            </otherwise>
        </choice>

        <!-- Belt-and-suspenders: pf-resolve-coordinates already raises if owner/repo is null,
             but an explicit check here makes the error origin unambiguous when pf-assess-app
             is called without going through pf-resolve-coordinates. -->
        <choice doc:name="CHOICE coords complete?"
                doc:id="pf000001-0001-0001-0001-000000000017"
                doc:description="Reject if owner or repo could not be resolved">
            <when expression="#[vars.coords.owner == null or vars.coords.repo == null]"
                  doc:name="owner or repo null"
                  doc:id="pf000001-0001-0001-0001-000000000018">
                <raise-error type="VALIDATION:APP_NOT_REGISTERED"
                             description="#['Cannot resolve repo coordinates for app &quot;' ++ vars.appName ++ '&quot;. Provide owner/repo in the registry or request body.']"
                             doc:name="RAISE VALIDATION:APP_NOT_REGISTERED"
                             doc:id="pf000001-0001-0001-0001-000000000019" />
            </when>
            <otherwise doc:name="coords present"
                       doc:id="pf000001-0001-0001-0001-000000000020">
                <logger level="DEBUG"
                        doc:name="LOG DEBUG coords present"
                        doc:id="pf000001-0001-0001-0001-00000000020b"
                        message="#['LOG DEBUG | flow=pf-assess-app | coords ok | owner=' ++ (vars.coords.owner default '') ++ ' | repo=' ++ (vars.coords.repo default '')]" />
            </otherwise>
        </choice>

        <!-- ── Step 3: GET HEAD commit SHA ──────────────────────────────────────────────────
             GET /repos/{owner}/{repo}/commits/{defaultBranch}
             Response: { sha, commit, ... }  →  vars.headSha = payload.sha
             ──────────────────────────────────────────────────────────────────────────────── -->
        <http:request method="GET"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/commits/' ++ vars.coords.defaultBranch]"
                      doc:name="GET GitHub commit SHA"
                      doc:id="pf000001-0001-0001-0001-000000000021"
                      doc:description="Fetch HEAD commit on defaultBranch to pin all Contents API calls to a consistent tree SHA" />

        <set-variable variableName="headSha"
                      value="#[payload.sha]"
                      doc:name="PV SET vars.headSha"
                      doc:id="pf000001-0001-0001-0001-000000000022"
                      doc:description="Store commit SHA; used as ref= parameter in all subsequent Contents API calls" />

        <!-- ── Step 4: GET full recursive tree ──────────────────────────────────────────────
             GET /repos/{owner}/{repo}/git/trees/{branch}?recursive=1
             Response: { tree: [{path, mode, type, sha, size, url}], truncated }
             →  vars.tree = full tree object
             ──────────────────────────────────────────────────────────────────────────────── -->
        <http:request method="GET"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/trees/' ++ vars.headSha]"
                      doc:name="GET GitHub tree (recursive)"
                      doc:id="pf000001-0001-0001-0001-000000000023"
                      doc:description="Fetch the full recursive file tree to locate pom.xml, mule-artifact.json, and CI workflow files">
            <http:query-params>#[{ recursive: "1" }]</http:query-params>
        </http:request>

        <set-variable variableName="tree"
                      value="#[payload]"
                      doc:name="PV SET vars.tree"
                      doc:id="pf000001-0001-0001-0001-000000000024"
                      doc:description="Store full tree object for path lookups in subsequent steps" />

        <!-- ── Step 5a: Analyse tree — locate pom, mule-artifact.json, CI workflow ──────────
             All tree-derived paths are computed in one DataWeave and stored as vars.treeAnalysis
             to avoid repeating identical tree scans in separate set-variable expressions.
             A second transform spreads the data bag into the flat variable names expected by
             the step-6 and step-7 DataWeave.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Analyse Tree"
                      doc:id="pf000001-0001-0001-0001-000000000030"
                      doc:description="Locate appPomPath, mule-artifact.json, CI workflow, and build allProps list from the recursive tree">
            <ee:variables>
                <ee:set-variable variableName="treeAnalysis"><![CDATA[%dw 2.0
output application/java
var appPath      = vars.coords.appPath default "."
var nominalPom   = if (appPath == ".") "pom.xml" else (appPath ++ "/pom.xml")
var treePaths    = vars.tree.tree map $.path
// Use nominal path when present; otherwise fall back to first blob pom.xml at any depth
var appPomPath   =
    if (treePaths contains nominalPom) nominalPom
    else ((vars.tree.tree filter ((item) ->
            item."type" == "blob" and item.path matches /(?:^|\/)pom\.xml$/
         ))[0].path) default nominalPom
var maPath       = if (appPath == ".") "mule-artifact.json" else (appPath ++ "/mule-artifact.json")
var maExists     = treePaths contains maPath
// CI workflow: first .github/workflows/*.yml or *.yaml found in tree
var ciPath       = ((vars.tree.tree filter ((item) ->
                       item.path matches /\.github\/workflows\/[^\/]+\.ya?ml$/
                   ))[0].path) default null
// All property names from matrix gating + connectors — needed by step-6 ownerOf()
var allProps     = ((valuesOf(vars.matrix.gating) map $.property)
                    ++ (vars.matrix.connectors map $.property))
---
{
    appPomPath:          appPomPath,
    muleArtifactExists:  maExists,
    muleArtifactPath:    if (maExists) maPath else null,
    ciWorkflowPath:      ciPath,
    allProps:            allProps
}]]></ee:set-variable>
            </ee:variables>
        </ee:transform>

        <!-- Spread data bag into flat variables expected by downstream transforms -->
        <ee:transform doc:name="DW Spread Tree Analysis to flat vars"
                      doc:id="pf000001-0001-0001-0001-000000000031">
            <ee:variables>
                <ee:set-variable variableName="appPomPath">#[vars.treeAnalysis.appPomPath]</ee:set-variable>
                <ee:set-variable variableName="muleArtifactExists">#[vars.treeAnalysis.muleArtifactExists]</ee:set-variable>
                <ee:set-variable variableName="muleArtifactPath">#[vars.treeAnalysis.muleArtifactPath]</ee:set-variable>
                <ee:set-variable variableName="ciWorkflowPath">#[vars.treeAnalysis.ciWorkflowPath]</ee:set-variable>
                <ee:set-variable variableName="allProps">#[vars.treeAnalysis.allProps]</ee:set-variable>
            </ee:variables>
        </ee:transform>

        <!-- ── Step 5b: Fetch app pom.xml via Contents API ───────────────────────────────────
             GET /repos/{owner}/{repo}/contents/{appPomPath}?ref={headSha}
             Response: { content: "<base64>", encoding: "base64", ... }
             Base64-decode → parse XML → init vars.chain[0], vars.appPomText, vars.nextParentPath
             ──────────────────────────────────────────────────────────────────────────────── -->
        <http:request method="GET"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/contents/' ++ vars.appPomPath]"
                      doc:name="GET app pom.xml (Contents API)"
                      doc:id="pf000001-0001-0001-0001-000000000032"
                      doc:description="Fetch app pom.xml via Contents API; response contains base64-encoded file content">
            <http:query-params>#[{ ref: vars.headSha }]</http:query-params>
        </http:request>

        <!-- Parse app pom: base64-decode, read XML, start chain, extract first parent path -->
        <ee:transform doc:name="DW Parse App Pom — init chain"
                      doc:id="pf000001-0001-0001-0001-000000000033"
                      doc:description="Base64-decode app pom content, parse XML, initialise vars.chain, compute next parent path">
            <ee:variables>
                <ee:set-variable variableName="appPomParsed"><![CDATA[%dw 2.0
output application/java
import fromBase64 from dw::core::Binaries
// Path normalisation: resolve relative parent paths against the directory of the current pom
fun removeLastSeg(arr: Array): Array =
    if (sizeOf(arr) <= 1) [] else arr[0 to (sizeOf(arr) - 2)]
fun normalizePath(currentPomPath: String, relPath: String): String = do {
    var dir      = if (currentPomPath contains "/")
                       currentPomPath[0 to ((currentPomPath lastIndexOf "/") - 1)]
                   else ""
    var combined = if (dir == "") relPath else (dir ++ "/" ++ relPath)
    var parts    = (combined splitBy "/") filter ($ != "" and $ != ".")
    --- (parts reduce ((seg, acc = []) ->
            if (seg == ".." and !isEmpty(acc)) removeLastSeg(acc)
            else acc ++ [seg]
        )) joinBy "/"
}
var rawBytes   = fromBase64(payload.content replace /[\r\n\t ]/ with "")
var rawContent = rawBytes as String {encoding: "UTF-8"}
var parsedPom  = read(rawContent, "application/xml")
var parentEl      = parsedPom.project.parent
var rawRel        = parentEl.relativePath default null
// Maven default: <parent> present but no <relativePath> ⇒ ../pom.xml
var relForResolve =
    if (parentEl == null) null
    else if (rawRel == null or (rawRel as String) == "") "../pom.xml"
    else (rawRel as String)
var resolved      = if (relForResolve == null) null
                    else normalizePath(vars.appPomPath, relForResolve)
// Directory-form relativePath (no .xml suffix) ⇒ append /pom.xml
var resolvedFile  = if (resolved == null) null
                    else if (resolved matches /.*pom\.xml$/) resolved
                    else (resolved ++ "/pom.xml")
// Only walk parents that live in THIS repo's tree; external/Exchange parents stop the chain
var treePaths     = vars.tree.tree map $.path
var nextPath      = if (resolvedFile != null and (treePaths contains resolvedFile)) resolvedFile
                    else null
---
{
    appPomText:      rawContent,
    chain:           [{ path: vars.appPomPath, pom: parsedPom }],
    nextParentPath:  nextPath
}]]></ee:set-variable>
            </ee:variables>
        </ee:transform>

        <!-- Spread parsed app pom data into flat variables -->
        <ee:transform doc:name="DW Spread App Pom to flat vars"
                      doc:id="pf000001-0001-0001-0001-000000000034">
            <ee:variables>
                <ee:set-variable variableName="appPomText">#[vars.appPomParsed.appPomText]</ee:set-variable>
                <ee:set-variable variableName="chain">#[vars.appPomParsed.chain]</ee:set-variable>
                <ee:set-variable variableName="nextParentPath">#[vars.appPomParsed.nextParentPath]</ee:set-variable>
            </ee:variables>
        </ee:transform>

        <!-- ── Step 5c: Walk parent chain — level 1 (direct parent) ─────────────────────────
             Fetch parent pom only if vars.nextParentPath was resolved from the app pom's
             <parent><relativePath>. Appends to vars.chain and recomputes nextParentPath.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <choice doc:name="CHOICE fetch parent pom (level 1)?"
                doc:id="pf000001-0001-0001-0001-000000000040"
                doc:description="Fetch direct parent pom when app pom declares a resolvable relativePath">
            <when expression="#[vars.nextParentPath != null]"
                  doc:name="parent pom exists"
                  doc:id="pf000001-0001-0001-0001-000000000041">

                <http:request method="GET"
                              config-ref="GitHub_Request"
                              path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/contents/' ++ vars.nextParentPath]"
                              doc:name="GET parent pom.xml level-1 (Contents API)"
                              doc:id="pf000001-0001-0001-0001-000000000042"
                              doc:description="Fetch the direct parent pom.xml via Contents API">
                    <http:query-params>#[{ ref: vars.headSha }]</http:query-params>
                </http:request>

                <!-- Parse parent-1 pom: base64-decode, read XML, append to chain, recompute nextParentPath -->
                <ee:transform doc:name="DW Parse Parent Pom level-1 — update chain"
                              doc:id="pf000001-0001-0001-0001-000000000043"
                              doc:description="Base64-decode parent-1 pom, parse XML, append to vars.chain, compute grandparent path">
                    <ee:variables>
                        <ee:set-variable variableName="parent1Data"><![CDATA[%dw 2.0
output application/java
import fromBase64 from dw::core::Binaries
fun removeLastSeg(arr: Array): Array =
    if (sizeOf(arr) <= 1) [] else arr[0 to (sizeOf(arr) - 2)]
fun normalizePath(currentPomPath: String, relPath: String): String = do {
    var dir      = if (currentPomPath contains "/")
                       currentPomPath[0 to ((currentPomPath lastIndexOf "/") - 1)]
                   else ""
    var combined = if (dir == "") relPath else (dir ++ "/" ++ relPath)
    var parts    = (combined splitBy "/") filter ($ != "" and $ != ".")
    --- (parts reduce ((seg, acc = []) ->
            if (seg == ".." and !isEmpty(acc)) removeLastSeg(acc)
            else acc ++ [seg]
        )) joinBy "/"
}
var rawBytes   = fromBase64(payload.content replace /[\r\n\t ]/ with "")
var rawContent = rawBytes as String {encoding: "UTF-8"}
var parsedPom  = read(rawContent, "application/xml")
var parentEl      = parsedPom.project.parent
var rawRel        = parentEl.relativePath default null
// Maven default: <parent> present but no <relativePath> ⇒ ../pom.xml
var relForResolve =
    if (parentEl == null) null
    else if (rawRel == null or (rawRel as String) == "") "../pom.xml"
    else (rawRel as String)
var resolved      = if (relForResolve == null) null
                    else normalizePath(vars.nextParentPath, relForResolve)
// Directory-form relativePath (no .xml suffix) ⇒ append /pom.xml
var resolvedFile  = if (resolved == null) null
                    else if (resolved matches /.*pom\.xml$/) resolved
                    else (resolved ++ "/pom.xml")
// Only walk parents that live in THIS repo's tree; external/Exchange parents stop the chain
var treePaths     = vars.tree.tree map $.path
var nextPath      = if (resolvedFile != null and (treePaths contains resolvedFile)) resolvedFile
                    else null
---
{
    chain:          vars.chain ++ [{ path: vars.nextParentPath, pom: parsedPom }],
    nextParentPath: nextPath
}]]></ee:set-variable>
                    </ee:variables>
                </ee:transform>

                <ee:transform doc:name="DW Spread Parent-1 to flat vars"
                              doc:id="pf000001-0001-0001-0001-000000000044">
                    <ee:variables>
                        <ee:set-variable variableName="chain">#[vars.parent1Data.chain]</ee:set-variable>
                        <ee:set-variable variableName="nextParentPath">#[vars.parent1Data.nextParentPath]</ee:set-variable>
                    </ee:variables>
                </ee:transform>

            </when>
            <otherwise doc:name="no direct parent (chain stops at app pom)"
                       doc:id="pf000001-0001-0001-0001-000000000045">
                <logger level="DEBUG"
                        doc:name="LOG DEBUG no level-1 parent"
                        doc:id="pf000001-0001-0001-0001-000000000046"
                        message="#['LOG DEBUG | flow=pf-assess-app | app=' ++ vars.appName ++ ' | chain depth=1 (APP_STANDALONE)']" />
            </otherwise>
        </choice>

        <!-- ── Step 5d: Walk parent chain — level 2 (grandparent / BOM) ────────────────────
             Only executed when the level-1 parent itself declared a further relativePath.
             After this step vars.chain is complete (max depth 3).
             ──────────────────────────────────────────────────────────────────────────────── -->
        <choice doc:name="CHOICE fetch grandparent pom (level 2)?"
                doc:id="pf000001-0001-0001-0001-000000000050"
                doc:description="Fetch grandparent (BOM) pom when the level-1 parent also declared a relativePath">
            <when expression="#[vars.nextParentPath != null]"
                  doc:name="grandparent pom exists"
                  doc:id="pf000001-0001-0001-0001-000000000051">

                <http:request method="GET"
                              config-ref="GitHub_Request"
                              path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/contents/' ++ vars.nextParentPath]"
                              doc:name="GET grandparent pom.xml level-2 (Contents API)"
                              doc:id="pf000001-0001-0001-0001-000000000052"
                              doc:description="Fetch the grandparent (typically BOM) pom.xml via Contents API">
                    <http:query-params>#[{ ref: vars.headSha }]</http:query-params>
                </http:request>

                <ee:transform doc:name="DW Parse Grandparent Pom level-2 — update chain"
                              doc:id="pf000001-0001-0001-0001-000000000053"
                              doc:description="Base64-decode grandparent pom, parse XML, append to vars.chain (chain is now complete)">
                    <ee:variables>
                        <ee:set-variable variableName="parent2Data"><![CDATA[%dw 2.0
output application/java
import fromBase64 from dw::core::Binaries
var rawBytes   = fromBase64(payload.content replace /[\r\n\t ]/ with "")
var rawContent = rawBytes as String {encoding: "UTF-8"}
var parsedPom  = read(rawContent, "application/xml")
---
{
    chain: vars.chain ++ [{ path: vars.nextParentPath, pom: parsedPom }]
}]]></ee:set-variable>
                    </ee:variables>
                </ee:transform>

                <ee:transform doc:name="DW Spread Grandparent to flat vars"
                              doc:id="pf000001-0001-0001-0001-000000000054">
                    <ee:variables>
                        <ee:set-variable variableName="chain">#[vars.parent2Data.chain]</ee:set-variable>
                        <ee:set-variable variableName="nextParentPath">#[null]</ee:set-variable>
                    </ee:variables>
                </ee:transform>

            </when>
            <otherwise doc:name="chain complete at depth ≤ 2"
                       doc:id="pf000001-0001-0001-0001-000000000055">
                <logger level="DEBUG"
                        doc:name="LOG DEBUG grandparent not present"
                        doc:id="pf000001-0001-0001-0001-000000000056"
                        message="#['LOG DEBUG | flow=pf-assess-app | app=' ++ vars.appName ++ ' | chain depth=' ++ sizeOf(vars.chain) ++ ' — no grandparent pom']" />
            </otherwise>
        </choice>

        <!-- ── Step 5e: Scan for flags — customJava, lookup(), ciWorkflow, warnings ─────────
             Lightweight static analysis using data already in memory (tree paths + appPomText).
             hasApiPolicies is a placeholder — a future step can call Anypoint API Manager.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Scan Flags"
                      doc:id="pf000001-0001-0001-0001-000000000060"
                      doc:description="Detect custom Java files, lookup() usage, and build warnings list from tree and pom text">
            <ee:variables>
                <ee:set-variable variableName="flags"><![CDATA[%dw 2.0
output application/java
// Custom Java: any .java file anywhere in the repository tree
var javaFiles      = vars.tree.tree filter ($.path matches /.*\.java$/)
var customJava     = !isEmpty(javaFiles)
// lookup(): scan raw app pom text as a fast heuristic; Mule XML scan would be more accurate
// but requires fetching every flow file — deferred to a future step
var lookupInPom    = vars.appPomText contains "lookup("
// Warnings
var truncWarning   = if (vars.tree.truncated default false)
                         ["Repository tree was truncated by GitHub (>100k objects); some file paths may have been missed."]
                     else []
var javaWarning    = if (customJava)
                         ["Custom Java classes detected (" ++ sizeOf(javaFiles) ++ " file(s)). Verify reflection and SecurityManager usage on JDK 17."]
                     else []
var lookupWarning  = if (lookupInPom)
                         ["lookup() reference found in pom text; scan Mule XMLs to confirm DataWeave POJO lookup usage requiring getter/setter validation."]
                     else []
---
{
    customJavaFound: customJava,
    lookupFound:     lookupInPom,
    hasApiPolicies:  false,    // TODO: query Anypoint API Manager for applied policies
    warnings:        truncWarning ++ javaWarning ++ lookupWarning
}]]></ee:set-variable>
            </ee:variables>
        </ee:transform>

        <ee:transform doc:name="DW Spread Flags to flat vars"
                      doc:id="pf000001-0001-0001-0001-000000000061">
            <ee:variables>
                <ee:set-variable variableName="customJavaFound">#[vars.flags.customJavaFound]</ee:set-variable>
                <ee:set-variable variableName="lookupFound">#[vars.flags.lookupFound]</ee:set-variable>
                <ee:set-variable variableName="hasApiPolicies">#[vars.flags.hasApiPolicies]</ee:set-variable>
                <ee:set-variable variableName="warnings">#[vars.flags.warnings]</ee:set-variable>
            </ee:variables>
        </ee:transform>

        <!-- ── Step 5f: Fetch mule-artifact.json content (for diff-aware app-level edit) ─────
             Only when the file exists. Parsed into vars.muleArtifactCurrent so step 7 can
             compare current minMuleVersion / javaSpecificationVersions against the target
             and emit the edit ONLY when it actually differs. Absent ⇒ var is null.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <choice doc:name="CHOICE fetch mule-artifact.json?"
                doc:id="pf000001-0001-0001-0001-0000000000a0"
                doc:description="Fetch and parse mule-artifact.json only when present in the tree">
            <when expression="#[vars.muleArtifactExists default false]"
                  doc:name="mule-artifact.json exists"
                  doc:id="pf000001-0001-0001-0001-0000000000a1">
                <http:request method="GET"
                              config-ref="GitHub_Request"
                              path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/contents/' ++ vars.muleArtifactPath]"
                              doc:name="GET mule-artifact.json (Contents API)"
                              doc:id="pf000001-0001-0001-0001-0000000000a2"
                              doc:description="Fetch mule-artifact.json content via Contents API">
                    <http:query-params>#[{ ref: vars.headSha }]</http:query-params>
                </http:request>
                <ee:transform doc:name="DW Decode mule-artifact.json"
                              doc:id="pf000001-0001-0001-0001-0000000000a3">
                    <ee:variables>
                        <ee:set-variable variableName="muleArtifactCurrent"><![CDATA[%dw 2.0
output application/java
import fromBase64 from dw::core::Binaries
---
read((fromBase64((payload.content default "") replace /[\r\n\t ]/ with "")) as String {encoding: "UTF-8"}, "application/json")]]></ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </when>
            <otherwise doc:name="no mule-artifact.json"
                       doc:id="pf000001-0001-0001-0001-0000000000a4">
                <ee:transform doc:name="DW mule-artifact null"
                              doc:id="pf000001-0001-0001-0001-0000000000a5">
                    <ee:variables>
                        <ee:set-variable variableName="muleArtifactCurrent">#[null]</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </otherwise>
        </choice>

        <!-- ── Step 5g: Fetch CI workflow content (for diff-aware app-level edit) ────────────
             Only when a workflow file was located. Raw text stored in vars.ciWorkflowText so
             step 7 can read the current java-version and emit the edit ONLY when it differs.
             Absent ⇒ var is null.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <choice doc:name="CHOICE fetch CI workflow?"
                doc:id="pf000001-0001-0001-0001-0000000000b0"
                doc:description="Fetch CI workflow text only when a workflow path was found">
            <when expression="#[vars.ciWorkflowPath != null]"
                  doc:name="CI workflow exists"
                  doc:id="pf000001-0001-0001-0001-0000000000b1">
                <http:request method="GET"
                              config-ref="GitHub_Request"
                              path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/contents/' ++ vars.ciWorkflowPath]"
                              doc:name="GET CI workflow (Contents API)"
                              doc:id="pf000001-0001-0001-0001-0000000000b2"
                              doc:description="Fetch CI workflow YAML content via Contents API">
                    <http:query-params>#[{ ref: vars.headSha }]</http:query-params>
                </http:request>
                <ee:transform doc:name="DW Decode CI workflow"
                              doc:id="pf000001-0001-0001-0001-0000000000b3">
                    <ee:variables>
                        <ee:set-variable variableName="ciWorkflowText"><![CDATA[%dw 2.0
output application/java
import fromBase64 from dw::core::Binaries
---
(fromBase64((payload.content default "") replace /[\r\n\t ]/ with "")) as String {encoding: "UTF-8"}]]></ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </when>
            <otherwise doc:name="no CI workflow"
                       doc:id="pf000001-0001-0001-0001-0000000000b4">
                <ee:transform doc:name="DW CI workflow null"
                              doc:id="pf000001-0001-0001-0001-0000000000b5">
                    <ee:variables>
                        <ee:set-variable variableName="ciWorkflowText">#[null]</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </otherwise>
        </choice>

        <!-- ── Step 6: Classify topology and build ownerByProperty map ──────────────────────
             topology   — shape of the POM chain: APP_STANDALONE | PARENT_APP | BOM_PARENT_APP | MULTI_LEVEL
             ownerByProperty — for each matrix property, the path of the POM that owns it
             (nearest-first search means app pom overrides win; the chain[0].path is the
             fallback edit target when a property is not yet declared anywhere in the chain).
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Classify Topology and build ownerByProperty"
                      doc:id="pf000001-0001-0001-0001-000000000070"
                      doc:description="Derive BOM/PARENT/STANDALONE topology from chain depth and build property→ownerPomPath map">
            <ee:variables>
                <ee:set-variable variableName="topoData"><![CDATA[%dw 2.0
output application/java
// vars.chain = ordered list of fetched POMs, nearest-first:
//   [{ path: "<app>/pom.xml", pom: <parsed> }, { path: "<parent>/pom.xml", pom },
//    { path: "<bom>/pom.xml", pom }]   // 1..N entries, ANY folder names
// classify by chain shape + whether the topmost is packaging=pom acting as a BOM
var n         = sizeOf(vars.chain)
var topIsBom  = (vars.chain[-1].pom.project.dependencyManagement?) != null
var topology  = if (n >= 3 and topIsBom) "BOM_PARENT_APP"
                else if (n == 2)          "PARENT_APP"
                else if (n == 1)          "APP_STANDALONE"
                else                      "MULTI_LEVEL"
// propOf(pom, prop): read a Maven <properties> value by NAME. Matches on the key
// as a plain String so dotted names (e.g. app.runtime.semver) and XML namespace
// Key types resolve reliably — a plain properties[prop] dynamic selector does not.
fun propOf(pom, prop) = do {
    var pairs = (pom.project.properties default {}) pluck ((pv, pk) -> { name: (pk as String), value: pv })
    --- (pairs filter ($.name == prop))[0].value default null
}
// ownerOf(prop): first POM in the chain (nearest-first) that declares the property.
// Falls back to the app POM so an edit always has a home (it can be ADDED there).
fun ownerOf(prop) =
    ((vars.chain filter ((c) -> propOf(c.pom, prop) != null))[0])
        .path default vars.chain[0].path
---
{
  topology: topology,
  appPomPath: vars.chain[0].path,
  ownerByProperty: {
    (vars.allProps map ((p) -> { (p): ownerOf(p) }))
  }
}]]></ee:set-variable>
            </ee:variables>
        </ee:transform>

        <ee:transform doc:name="DW Spread Topology to flat vars"
                      doc:id="pf000001-0001-0001-0001-000000000071">
            <ee:variables>
                <ee:set-variable variableName="topology">#[vars.topoData.topology]</ee:set-variable>
                <ee:set-variable variableName="ownerByProperty">#[vars.topoData.ownerByProperty]</ee:set-variable>
            </ee:variables>
        </ee:transform>

        <!-- ── Step 7: Build AssessmentResult payload ────────────────────────────────────────
             Applies all matrix gating + connector rules against installed property values,
             each carrying its owner pom path. Emits the full changePlan with fileEdits list.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Build AssessmentResult"
                      doc:id="pf000001-0001-0001-0001-000000000080"
                      doc:description="Apply all matrix upgrade rules to installed property values and produce the full AssessmentResult JSON payload">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
import * from dw::core::Strings
// helpers: simple semver compare a < b
fun toNums(v) = (v splitBy ".") map (trim($) replace /[^0-9].*/ with "") map (($ default "0") as Number)
fun lt(a,b) = do { var x=toNums(a)
    var y=toNums(b)
    --- (x[0] default 0) < (y[0] default 0) or
        ((x[0] default 0)==(y[0] default 0) and (x[1] default 0) < (y[1] default 0)) or
        ((x[0] default 0)==(y[0] default 0) and (x[1] default 0)==(y[1] default 0) and (x[2] default 0)<(y[2] default 0)) }
var m = vars.matrix
// propOf(pom, prop): read a Maven <properties> value by NAME (String-key match) so
// dotted names and XML namespace Key types resolve reliably.
fun propOf(pom, prop) = do {
    var pairs = (pom.project.properties default {}) pluck ((pv, pk) -> { name: (pk as String), value: pv })
    --- (pairs filter ($.name == prop))[0].value default null
}
// rawProp(name): first non-null property value across the chain (nearest-first), no indirection.
fun rawProp(name) = ((vars.chain map ((c) -> propOf(c.pom, name))) filter ($ != null))[0] default null
// resolveProp(name): rawProp, following ONE level of Maven property-placeholder indirection when present.
fun resolveProp(name) = do {
    var v = rawProp(name)
    --- if (v != null and ((v as String) matches /^\s*\$\{.+\}\s*$/))
            rawProp((trim(v as String) replace /^\$\{/ with "" replace /\}$/ with ""))
        else v
}
// ownerOfProp(name): path of the nearest chain pom that declares the property, else null.
fun ownerOfProp(name) =
    ((vars.chain filter ((c) -> propOf(c.pom, name) != null))[0]).path default null
// findDep(g,a): nearest chain entry declaring dependency g:a in <dependencies> or
// <dependencyManagement>, returned as { path, dep }, else null.
fun findDep(g, a) =
    (flatten(vars.chain map ((c) -> do {
        var deps = ((c.pom.project.dependencies default {}).*dependency) default []
        var mgmt = ((c.pom.project.dependencyManagement.dependencies default {}).*dependency) default []
        --- (deps ++ mgmt) map ((d) -> { path: c.path, dep: d })
    })) filter ((x) ->
        (((x.dep.groupId default "") as String) == g) and
        (((x.dep.artifactId default "") as String) == a)))[0] default null
// findPlugin(g,a): nearest chain entry declaring plugin a in <build><plugins> or
// <build><pluginManagement><plugins>, returned as { path, plugin }, else null.
// groupId is optional (plugins are commonly identified by artifactId alone).
fun findPlugin(g, a) =
    (flatten(vars.chain map ((c) -> do {
        var ps  = ((c.pom.project.build.plugins default {}).*plugin) default []
        var pms = ((c.pom.project.build.pluginManagement.plugins default {}).*plugin) default []
        --- (ps ++ pms) map ((p) -> { path: c.path, plugin: p })
    })) filter ((x) ->
        (((x.plugin.artifactId default "") as String) == a) and
        (g == null or (((x.plugin.groupId default "") as String) == g))))[0] default null
// resolveInline(r, ver, path, kind, coords): shared handling for an inline <version> found
// on a dependency or plugin — null → skip; property-placeholder → resolve as pomProperty on
// the referenced property; literal → emit `kind` carrying the given coordinate fields.
fun resolveInline(r, ver, path, kind, coords) =
    if (ver == null) { property: r.property, kind: null, file: null, installed: null }
    else if ((ver as String) matches /^\s*\$\{.+\}\s*$/) do {
        var pn = (trim(ver as String) replace /^\$\{/ with "" replace /\}$/ with "")
        --- { property: pn, kind: "pomProperty", file: (ownerOfProp(pn) default path), installed: rawProp(pn) }
    }
    else
        ({ property: r.property, kind: kind, file: path, installed: (ver as String) } ++ coords)
// resolveRule(r): find installed value + owner file + edit kind. Resolution order:
//   1) <properties> value (kind=pomProperty)
//   2) inline <dependency>/<dependencyManagement> <version> (kind=depVersion)
//   3) inline <plugin> <version> (kind=pluginVersion)
// An inline value that is itself a property placeholder collapses back to a pomProperty edit.
fun resolveRule(r) = do {
    var pOwner = ownerOfProp(r.property)
    var depHit = if ((r.groupId?) and (r.artifactId?)) findDep(r.groupId, r.artifactId) else null
    var plgHit = if (r.pluginArtifactId?) findPlugin((r.pluginGroupId default null), r.pluginArtifactId) else null
    ---
    if (pOwner != null)
        { property: r.property, kind: "pomProperty", file: pOwner, installed: rawProp(r.property) }
    else if (depHit != null)
        resolveInline(r, (depHit.dep.version default null), depHit.path, "depVersion",
                      { groupId: r.groupId, artifactId: r.artifactId })
    else if (plgHit != null)
        resolveInline(r, (plgHit.plugin.version default null), plgHit.path, "pluginVersion",
                      { pluginGroupId: (r.pluginGroupId default null), pluginArtifactId: r.pluginArtifactId })
    else { property: r.property, kind: null, file: null, installed: null }
}
// all gating + connector rules → one list; each edit carries its OWN owner file + kind
var allRules = (valuesOf(m.gating) ++ m.connectors)
var propEdits = allRules
    map ((r) -> do {
        var res       = resolveRule(r)
        var installed = res.installed
        var needs     = if (installed == null or res.kind == null) false   // not present → skip
                        else if (r.in?)  (r.in contains installed)
                        else lt(installed, r.set)
        ---
        {
            property:         res.property,
            kind:             res.kind,
            file:             res.file,
            (groupId:          res.groupId)          if (res.groupId?),
            (artifactId:       res.artifactId)       if (res.artifactId?),
            (pluginGroupId:    res.pluginGroupId)    if (res.pluginGroupId?),
            (pluginArtifactId: res.pluginArtifactId) if (res.pluginArtifactId?),
            from:             installed,
            to:               r.set,
            change:           needs
        } })
    filter ($.change)
// app-level edits — DIFF-AWARE: emit ONLY when the current value actually differs from target.
var appPomText = vars.appPomText default ""
// (1) MUnit <runtimeVersion> — literal only. A property-placeholder value is driven by a
//     property, so it is handled by the property path above and must NOT be rewritten here.
var munitCur         = (appPomText scan /<runtimeVersion>\s*([^<]+?)\s*<\/runtimeVersion>/)[0][1] default null
var munitPlaceholder = (munitCur != null) and ((munitCur default "") matches /^\s*\$\{.+\}\s*$/)
var munitNeeds       = (munitCur != null) and (!munitPlaceholder) and lt((munitCur as String), m.target.runtime)
// (2) mule-artifact.json — bump minMuleVersion only if BELOW target (never downgrade); ensure
//     java target support only if no current spec is already >= target (handles "1.8"/"8"/"11").
var maCur      = vars.muleArtifactCurrent
var maSpecs    = (maCur.javaSpecificationVersions default [])
var maMinNeeds = (maCur != null) and lt((maCur.minMuleVersion default "0"), m.muleArtifact.minMuleVersion)
var maJavaOk   = !isEmpty(maSpecs filter ((s) -> !lt((s as String), m.target.javaVersion)))
var maJavaNeeds= (maCur != null) and (!maJavaOk)
var maNeeds    = maMinNeeds or maJavaNeeds
// Never downgrade minMuleVersion: keep current when it is already at/above target.
var maToMin    = if (maMinNeeds) m.muleArtifact.minMuleVersion else (maCur.minMuleVersion default m.muleArtifact.minMuleVersion)
// (3) CI workflow — bump only if the current java-version is below target (never downgrade).
var ciCur      = ((vars.ciWorkflowText default "") scan /java-version:\s*['"]?([^'"\s]+)['"]?/)[0][1] default null
var ciNeeds    = (ciCur != null) and lt((ciCur as String), m.target.javaVersion)
var appEdits = []
  ++ (if (munitNeeds)
        [{ file: vars.chain[0].path, kind: "munitRuntimeVersion", from: munitCur, to: m.target.runtime }] else [])
  ++ (if (maNeeds)
        [{ file: vars.muleArtifactPath, kind: "muleArtifactJson",
           from: { minMuleVersion: (maCur.minMuleVersion default null), javaSpecificationVersions: maSpecs },
           to: { minMuleVersion: maToMin,
                 javaSpecificationVersions: m.muleArtifact.javaSpecificationVersions } }] else [])
  ++ (if (ciNeeds)
        [{ file: vars.ciWorkflowPath, kind: "ciWorkflow", from: ciCur, to: m.target.javaVersion }] else [])
var all = propEdits ++ appEdits
---
{
  appName: vars.appName,
  currentRuntime: resolveProp("app.runtime") default resolveProp("app.runtime.semver") default "unknown",
  currentJavaVersion: resolveProp("java.version") default resolveProp("maven.compiler.source") default resolveProp("maven.compiler.target") default "unknown",
  changePlan: {
    targetRuntime:     m.target.runtime,
    targetJavaVersion: m.target.javaVersion,
    topology:          vars.topology,
    headSha:           vars.headSha,
    fileEdits:         all,
    filesToChange:     (all map $.file) distinctBy $,
    hasApiPolicies:    vars.hasApiPolicies default false,
    hasCustomJavaCode: vars.customJavaFound default false,
    hasLookupFunction: vars.lookupFound default false
  },
  warnings: vars.warnings default []
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <logger level="DEBUG"
                doc:name="LOG DEBUG pf-assess-app done"
                doc:id="pf000001-0001-0001-0001-000000000090"
                message="#['LOG DEBUG | flow=pf-assess-app | correlationId=' ++ correlationId ++ ' | app=' ++ vars.appName ++ ' | topology=' ++ (vars.topology default 'unknown') ++ ' | fileEdits=' ++ sizeOf(payload.changePlan.fileEdits default []) ++ ' | warnings=' ++ sizeOf(payload.warnings default [])]" />

    </sub-flow>

    <!-- pf-start-upgrade
         Requires: vars.appName, vars.environment, vars.jiraTicketId (opt), vars.approvedChangePlan (opt for audit)
         Returns:  payload = { jobId }  (synchronous — 202 Accepted)
         Kicks off an <async> upgrade pipeline after returning the jobId. -->
    <sub-flow name="pf-start-upgrade"
              doc:id="pf000002-0002-0002-0002-000000000001"
              doc:name="pf-start-upgrade"
              doc:description="Generate jobId, acquire single-flight lock, store job, return 202, then run async pipeline.">

        <logger level="INFO"
                doc:name="LOG INFO pf-start-upgrade enter"
                doc:id="pf000002-0002-0002-0002-000000000002"
                message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | app=' ++ (vars.appName default 'unknown') ++ ' | env=' ++ (vars.environment default 'unknown')]" />

        <!-- Step 1: Assign jobId -->
        <set-variable variableName="jobId"
                      value="#['job-' ++ uuid()]"
                      doc:name="PV SET jobId"
                      doc:id="pf000002-0002-0002-0002-000000000003" />

        <!-- Step 2: Resolve repo coordinates (fails fast on allow-list violations) -->
        <flow-ref name="pf-resolve-coordinates"
                  doc:name="pf-resolve-coordinates"
                  doc:id="pf000002-0002-0002-0002-000000000010" />

        <logger level="INFO"
                doc:name="LOG INFO coords resolved"
                doc:id="pf000002-0002-0002-0002-000000000011"
                message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | owner=' ++ (vars.coords.owner default '') ++ ' | repo=' ++ (vars.coords.repo default '')]" />

        <!-- Step 3: Single-flight lock — prevent concurrent upgrades for the same app -->
        <os:retrieve key="#['lock::' ++ vars.appName]"
                     objectStore="jobStore"
                     target="existingLock"
                     doc:name="OS Retrieve upgrade lock"
                     doc:id="pf000002-0002-0002-0002-000000000012">
            <os:default-value>#[null]</os:default-value>
        </os:retrieve>

        <choice doc:name="CHOICE lock held?"
                doc:id="pf000002-0002-0002-0002-000000000013"
                doc:description="Reject concurrent upgrades for the same app">
            <when expression="#[vars.existingLock != null]"
                  doc:name="lock already held"
                  doc:id="pf000002-0002-0002-0002-000000000014">
                <logger level="WARN"
                        doc:name="LOG WARN upgrade in progress"
                        doc:id="pf000002-0002-0002-0002-000000000015"
                        message="#['LOG WARN | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | app=' ++ vars.appName ++ ' already has an upgrade in progress — jobId=' ++ (vars.existingLock as String)]" />
                <raise-error type="CONFLICT:UPGRADE_IN_PROGRESS"
                             description="#['An upgrade for app &quot;' ++ vars.appName ++ '&quot; is already in progress (jobId=' ++ (vars.existingLock as String) ++ '). Wait for it to complete or fail before starting a new one.']"
                             doc:name="RAISE CONFLICT:UPGRADE_IN_PROGRESS"
                             doc:id="pf000002-0002-0002-0002-000000000016" />
            </when>
            <otherwise doc:name="lock available — acquire it"
                       doc:id="pf000002-0002-0002-0002-000000000017">
                <os:store key="#['lock::' ++ vars.appName]"
                          objectStore="jobStore"
                          doc:name="OS Store upgrade lock"
                          doc:id="pf000002-0002-0002-0002-000000000018">
                    <os:value>#[vars.jobId]</os:value>
                </os:store>
                <logger level="INFO"
                        doc:name="LOG INFO lock acquired"
                        doc:id="pf000002-0002-0002-0002-000000000019"
                        message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | lock acquired for app=' ++ vars.appName]" />
            </otherwise>
        </choice>

        <!-- Step 4: Persist initial job record (includes coords for downstream use) -->
        <ee:transform doc:name="DW Build Job Record"
                      doc:id="pf000002-0002-0002-0002-000000000004">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    jobId:              vars.jobId,
    status:             "PROCESSING",
    appName:            vars.appName,
    environment:        vars.environment,
    jiraTicketId:       vars.jiraTicketId,
    approvedChangePlan: vars.approvedChangePlan,
    coords:             vars.coords,
    createdAt:          now() as String {format: "yyyy-MM-dd'T'HH:mm:ss'Z'"},
    updatedAt:          now() as String {format: "yyyy-MM-dd'T'HH:mm:ss'Z'"},
    prUrl:              null,
    branchName:         null,
    completedAt:        null,
    error:              null
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <os:store key="#[vars.jobId]"
                  objectStore="jobStore"
                  doc:name="OS Store Job Record"
                  doc:id="pf000002-0002-0002-0002-000000000005">
            <os:value>#[payload]</os:value>
        </os:store>

        <logger level="INFO"
                doc:name="LOG INFO job record stored"
                doc:id="pf000002-0002-0002-0002-000000000006"
                message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | status=PROCESSING | app=' ++ vars.appName]" />

        <!-- Step 5: Return 202 immediately — caller gets jobId before async work starts -->
        <ee:transform doc:name="DW Return jobId"
                      doc:id="pf000002-0002-0002-0002-000000000007">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ jobId: vars.jobId }]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <!-- Step 6: Async upgrade pipeline — runs after the 202 is already on the wire -->
        <async doc:name="ASYNC upgrade pipeline"
               doc:id="pf000002-0002-0002-0002-000000000020">
            <try doc:name="TRY upgrade stages"
                 doc:id="pf000002-0002-0002-0002-000000000021">

                <!-- Stage a: assess -->
                <set-variable variableName="targetStatus"
                              value="ASSESSING"
                              doc:name="PV SET targetStatus=ASSESSING"
                              doc:id="pf000002-0002-0002-0002-000000000030" />
                <flow-ref name="pf-set-status"
                          doc:name="pf-set-status (ASSESSING)"
                          doc:id="pf000002-0002-0002-0002-000000000031" />
                <flow-ref name="pf-assess-app"
                          doc:name="pf-assess-app"
                          doc:id="pf000002-0002-0002-0002-000000000032" />
                <logger level="INFO"
                        doc:name="LOG INFO assessment complete"
                        doc:id="pf000002-0002-0002-0002-000000000033"
                        message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | assessment complete']" />

                <!-- Required handoff: downstream flows read vars.changePlan, not payload -->
                <set-variable variableName="changePlan"
                              value="#[payload.changePlan]"
                              doc:name="PV SET changePlan (from AssessmentResult)"
                              doc:id="pf000002-0002-0002-0002-000000000034" />

                <!-- Stage d: apply transforms -->
                <set-variable variableName="targetStatus"
                              value="COMMITTING"
                              doc:name="PV SET targetStatus=COMMITTING"
                              doc:id="pf000002-0002-0002-0002-000000000035" />
                <flow-ref name="pf-set-status"
                          doc:name="pf-set-status (COMMITTING)"
                          doc:id="pf000002-0002-0002-0002-000000000036" />
                <flow-ref name="pf-apply-transforms"
                          doc:name="pf-apply-transforms"
                          doc:id="pf000002-0002-0002-0002-000000000037" />
                <logger level="INFO"
                        doc:name="LOG INFO transforms applied"
                        doc:id="pf000002-0002-0002-0002-000000000038"
                        message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | stagedFiles=' ++ sizeOf(vars.stagedFiles default [])]" />

                <!-- Stage e: commit -->
                <flow-ref name="pf-atomic-commit"
                          doc:name="pf-atomic-commit"
                          doc:id="pf000002-0002-0002-0002-000000000039" />
                <set-variable variableName="targetStatus"
                              value="COMMITTED"
                              doc:name="PV SET targetStatus=COMMITTED"
                              doc:id="pf000002-0002-0002-0002-000000000040" />
                <flow-ref name="pf-set-status"
                          doc:name="pf-set-status (COMMITTED)"
                          doc:id="pf000002-0002-0002-0002-000000000041" />
                <logger level="INFO"
                        doc:name="LOG INFO committed"
                        doc:id="pf000002-0002-0002-0002-000000000042"
                        message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | changes committed to branch']" />

                <!-- Stage f: open PR -->
                <flow-ref name="pf-open-pr"
                          doc:name="pf-open-pr"
                          doc:id="pf000002-0002-0002-0002-000000000043" />
                <set-variable variableName="targetStatus"
                              value="PR_OPEN"
                              doc:name="PV SET targetStatus=PR_OPEN"
                              doc:id="pf000002-0002-0002-0002-000000000044" />
                <flow-ref name="pf-set-status"
                          doc:name="pf-set-status (PR_OPEN)"
                          doc:id="pf000002-0002-0002-0002-000000000045" />
                <logger level="INFO"
                        doc:name="LOG INFO PR open"
                        doc:id="pf000002-0002-0002-0002-000000000046"
                        message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | PR opened']" />

                <!-- Stage g: notify success -->
                <flow-ref name="pf-notify"
                          doc:name="pf-notify"
                          doc:id="pf000002-0002-0002-0002-000000000047" />

                <!-- Error handler: never leave a job in PROCESSING -->
                <error-handler>
                    <on-error-continue type="ANY"
                                       doc:name="handle any async failure"
                                       doc:id="pf000002-0002-0002-0002-000000000050">

                        <logger level="ERROR"
                                doc:name="LOG ERROR async stage failed"
                                doc:id="pf000002-0002-0002-0002-000000000051"
                                message="#['LOG ERROR | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | errorType=' ++ (error.errorType.namespace default '') ++ ':' ++ (error.errorType.identifier default '') ++ ' | ' ++ (error.description default '')]" />

                        <!-- Classify failure status from error type -->
                        <ee:transform doc:name="DW Determine failureStatus"
                                      doc:id="pf000002-0002-0002-0002-000000000052">
                            <ee:variables>
                                <ee:set-variable variableName="failureStatus"><![CDATA[%dw 2.0
output application/java
---
if ((error.errorType.namespace == "VALIDATION") or (error.errorType.namespace == "HTTP"))
    "FAILED_ASSESS"
else "FAILED_COMMIT"]]></ee:set-variable>
                            </ee:variables>
                        </ee:transform>

                        <!-- Retrieve job record, merge failure fields, re-store -->
                        <os:retrieve key="#[vars.jobId]"
                                     objectStore="jobStore"
                                     doc:name="OS Retrieve job for failure update"
                                     doc:id="pf000002-0002-0002-0002-000000000053">
                            <os:default-value>#[{}]</os:default-value>
                        </os:retrieve>

                        <ee:transform doc:name="DW Merge failure into job record"
                                      doc:id="pf000002-0002-0002-0002-000000000054">
                            <ee:message>
                                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
payload ++ {
    status:    vars.failureStatus,
    error:     error.description default "unknown error",
    updatedAt: now() as String {format: "yyyy-MM-dd'T'HH:mm:ss'Z'"}
}]]></ee:set-payload>
                            </ee:message>
                        </ee:transform>

                        <os:store key="#[vars.jobId]"
                                  objectStore="jobStore"
                                  doc:name="OS Store failed job record"
                                  doc:id="pf000002-0002-0002-0002-000000000055">
                            <os:value>#[payload]</os:value>
                        </os:store>

                        <!-- Release the lock so a retry is possible -->
                        <os:remove key="#['lock::' ++ vars.appName]"
                                   objectStore="jobStore"
                                   doc:name="OS Remove upgrade lock (on failure)"
                                   doc:id="pf000002-0002-0002-0002-000000000056" />

                        <logger level="INFO"
                                doc:name="LOG INFO job marked failed + lock released"
                                doc:id="pf000002-0002-0002-0002-000000000057"
                                message="#['LOG INFO | flow=pf-start-upgrade | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | status=' ++ (vars.failureStatus default 'FAILED') ++ ' | lock released']" />

                        <flow-ref name="pf-notify-failure"
                                  doc:name="pf-notify-failure"
                                  doc:id="pf000002-0002-0002-0002-000000000058" />

                    </on-error-continue>
                </error-handler>
            </try>
        </async>

    </sub-flow>

    <!-- ==========================================================================
         pf-set-status
         Reads vars.targetStatus + vars.jobId; merges status + updatedAt onto the
         persisted job record and re-stores it (rolling TTL). Transport-agnostic.
         ========================================================================== -->
    <sub-flow name="pf-set-status"
              doc:id="pfss0001-0001-0001-0001-000000000001"
              doc:name="pf-set-status"
              doc:description="Merge vars.targetStatus + updatedAt into the persisted job record.">

        <logger level="DEBUG"
                doc:name="LOG DEBUG pf-set-status"
                doc:id="pfss0001-0001-0001-0001-000000000002"
                message="#['LOG DEBUG | flow=pf-set-status | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | targetStatus=' ++ (vars.targetStatus default '?')]" />

        <os:retrieve key="#[vars.jobId]"
                     objectStore="jobStore"
                     doc:name="OS Retrieve job for status update"
                     doc:id="pfss0001-0001-0001-0001-000000000003">
            <os:default-value>#[{}]</os:default-value>
        </os:retrieve>

        <ee:transform doc:name="DW Merge status + updatedAt"
                      doc:id="pfss0001-0001-0001-0001-000000000004">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
payload ++ {
    status:    vars.targetStatus,
    updatedAt: now() as String {format: "yyyy-MM-dd'T'HH:mm:ss'Z'"}
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <os:store key="#[vars.jobId]"
                  objectStore="jobStore"
                  doc:name="OS Store updated job status"
                  doc:id="pfss0001-0001-0001-0001-000000000005">
            <os:value>#[payload]</os:value>
        </os:store>

    </sub-flow>

    <!-- ==========================================================================
         pf-atomic-commit
         Commits all vars.stagedFiles as ONE commit on a new branch via the GitHub
         Git Data API (low-level: blob → tree → commit → update ref).
         Inputs:  vars.coords {owner,repo,defaultBranch}
                  vars.changePlan {headSha,targetRuntime,targetJavaVersion}
                  vars.stagedFiles [{path,content}]
                  vars.appName, vars.jobId, vars.jiraTicketId (optional)
         Outputs: vars.branchName, vars.commitSha written to job record in OS.
                  Status is NOT changed here — pf-start-upgrade sets COMMITTED.
         ========================================================================== -->
    <sub-flow name="pf-atomic-commit"
              doc:id="pfac0001-0001-0001-0001-000000000001"
              doc:name="pf-atomic-commit"
              doc:description="Commit all staged files as one Git commit on a new branch via GitHub Git Data API.">

        <logger level="INFO"
                doc:name="LOG INFO pf-atomic-commit enter"
                doc:id="pfac0001-0001-0001-0001-000000000002"
                message="#['LOG INFO | flow=pf-atomic-commit | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | stagedFiles=' ++ sizeOf(vars.stagedFiles default []) ++ ' | headSha=' ++ (vars.changePlan.headSha default '?')]" />

        <!-- ── Step 1: Stale-plan guard ───────────────────────────────────────────────────
             Verify the branch HEAD has not moved since assessment. If it has, the
             changePlan's fileEdits and headSha may no longer apply cleanly — re-assess.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <http:request method="GET"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/commits/' ++ vars.coords.defaultBranch]"
                      target="headCheck"
                      doc:name="GET HEAD commit (stale-plan check)"
                      doc:id="pfac0001-0001-0001-0001-000000000003"
                      doc:description="Fetch current HEAD SHA to verify the repo has not moved since the change plan was computed" />

        <choice doc:name="CHOICE stale plan?"
                doc:id="pfac0001-0001-0001-0001-000000000004"
                doc:description="Reject if the branch HEAD has moved since pf-assess-app captured it">
            <when expression="#[vars.headCheck.sha != vars.changePlan.headSha]"
                  doc:name="HEAD moved — plan is stale"
                  doc:id="pfac0001-0001-0001-0001-000000000005">
                <logger level="WARN"
                        doc:name="LOG WARN stale plan"
                        doc:id="pfac0001-0001-0001-0001-000000000006"
                        message="#['LOG WARN | flow=pf-atomic-commit | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | stale plan: repo HEAD=' ++ (vars.headCheck.sha default '?') ++ ' != plan headSha=' ++ (vars.changePlan.headSha default '?')]" />
                <raise-error type="CONFLICT:STALE_PLAN"
                             description="#['The repo HEAD moved since assessment (current=' ++ (vars.headCheck.sha default '?') ++ ', plan=' ++ (vars.changePlan.headSha default '?') ++ '). Re-run assess_app to get a fresh changePlan.']"
                             doc:name="RAISE CONFLICT:STALE_PLAN"
                             doc:id="pfac0001-0001-0001-0001-000000000007" />
            </when>
            <otherwise doc:name="plan is fresh — proceed"
                       doc:id="pfac0001-0001-0001-0001-000000000008">
                <logger level="DEBUG"
                        doc:name="LOG DEBUG stale-plan check passed"
                        doc:id="pfac0001-0001-0001-0001-000000000008a"
                        message="#['LOG DEBUG | flow=pf-atomic-commit | stale-plan check passed | headSha matches plan']" />
            </otherwise>
        </choice>

        <!-- ── Step 2: Compute a COLLISION-FREE branch name ─────────────────────────────────
             Base name is deterministic: migrate/{appName}-{targetRuntime}-java{targetJavaVersion}.
             If that (or a numbered variant) already exists on the remote, pick the first free
             suffix (-1, -2, …) so re-runs never fail with 422 and never force-update an
             existing branch. Existing names are discovered via the Git matching-refs API.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <set-variable variableName="branchBase"
                      value="#['migrate/' ++ vars.appName ++ '-' ++ vars.changePlan.targetRuntime ++ '-java' ++ vars.changePlan.targetJavaVersion]"
                      doc:name="PV SET branchBase"
                      doc:id="pfac0001-0001-0001-0001-000000000009"
                      doc:description="Deterministic base branch name before collision handling" />

        <!-- GET all refs whose name starts with the base — returns [] (200) when none exist. -->
        <http:request method="GET"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/matching-refs/heads/' ++ vars.branchBase]"
                      target="existingRefs"
                      doc:name="GET matching branch refs"
                      doc:id="pfac0001-0001-0001-0001-00000000000d"
                      doc:description="List existing branches sharing the base name so a free suffix can be chosen" />

        <ee:transform doc:name="DW Pick free branch name"
                      doc:id="pfac0001-0001-0001-0001-00000000000e">
            <ee:variables>
                <ee:set-variable variableName="branchName"><![CDATA[%dw 2.0
output application/java
var base       = vars.branchBase
// names already taken on the remote (strip the refs/heads/ prefix)
var taken      = (vars.existingRefs default []) map ((r) -> ((r.ref as String) replace /^refs\/heads\// with ""))
// try the base first, then base-1 .. base-50
var candidates = [base] ++ ((1 to 50) map ((n) -> base ++ "-" ++ (n as String)))
---
(candidates filter ((c) -> !(taken contains c)))[0] default (base ++ "-" ++ vars.jobId)]]></ee:set-variable>
            </ee:variables>
        </ee:transform>

        <logger level="INFO"
                doc:name="LOG INFO branch name computed"
                doc:id="pfac0001-0001-0001-0001-000000000010"
                message="#['LOG INFO | flow=pf-atomic-commit | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | branchBase=' ++ vars.branchBase ++ ' | branchName=' ++ vars.branchName ++ ' | existing=' ++ sizeOf(vars.existingRefs default [])]" />

        <!-- ── Step 3: Create branch ref ──────────────────────────────────────────────────
             POST /repos/{owner}/{repo}/git/refs
             Body: { ref: "refs/heads/{branchName}", sha: {headSha} }
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Build create-ref body"
                      doc:id="pfac0001-0001-0001-0001-000000000011">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    ref: ("refs/heads/" ++ vars.branchName),
    sha: vars.changePlan.headSha
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <http:request method="POST"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/refs']"
                      doc:name="POST create branch ref"
                      doc:id="pfac0001-0001-0001-0001-000000000012"
                      doc:description="Create the upgrade branch at the current HEAD SHA" />

        <!-- ── Step 4: Create one blob per staged file ────────────────────────────────────
             POST /repos/{owner}/{repo}/git/blobs  for each {path, content} in stagedFiles.
             Accumulates [{path, blobSha}] into vars.blobs for the tree step.
             Requests are sequential (default foreach) to respect GitHub rate limits.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <set-variable variableName="blobs"
                      value="#[[]]"
                      doc:name="PV INIT blobs accumulator"
                      doc:id="pfac0001-0001-0001-0001-000000000013" />

        <foreach collection="#[vars.stagedFiles]"
                 doc:name="FOR EACH staged file — create blob"
                 doc:id="pfac0001-0001-0001-0001-000000000014">

            <set-variable variableName="currentFile"
                          value="#[payload]"
                          doc:name="PV SET currentFile"
                          doc:id="pfac0001-0001-0001-0001-000000000015" />

            <ee:transform doc:name="DW Build create-blob body"
                          doc:id="pfac0001-0001-0001-0001-000000000016">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    content:  vars.currentFile.content,
    encoding: "utf-8"
}]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <http:request method="POST"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/blobs']"
                          target="blobResp"
                          doc:name="POST create blob"
                          doc:id="pfac0001-0001-0001-0001-000000000017"
                          doc:description="Upload file content as a Git blob; response contains the blob SHA" />

            <set-variable variableName="blobs"
                          value="#[(vars.blobs default []) ++ [{ path: vars.currentFile.path, blobSha: vars.blobResp.sha }]]"
                          doc:name="PV APPEND blob to accumulator"
                          doc:id="pfac0001-0001-0001-0001-000000000018" />

        </foreach>

        <!-- ── Step 5: Create tree ────────────────────────────────────────────────────────
             POST /repos/{owner}/{repo}/git/trees
             Body: { base_tree: {headSha}, tree: [{path, mode, type, sha}] }
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Build create-tree body"
                      doc:id="pfac0001-0001-0001-0001-000000000019">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    base_tree: vars.changePlan.headSha,
    tree: vars.blobs map ((b) -> {
        path: b.path,
        mode: "100644",
        "type": "blob",
        sha:  b.blobSha
    })
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <http:request method="POST"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/trees']"
                      target="treeResp"
                      doc:name="POST create tree"
                      doc:id="pfac0001-0001-0001-0001-000000000020"
                      doc:description="Create a new Git tree from blobs; response contains the tree SHA" />

        <!-- ── Step 6: Create commit ──────────────────────────────────────────────────────
             POST /repos/{owner}/{repo}/git/commits
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Build create-commit body"
                      doc:id="pfac0001-0001-0001-0001-000000000021">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    message: ("Java 17 upgrade (" ++ vars.jobId ++ ")"
              ++ (if (vars.jiraTicketId != null) (" " ++ vars.jiraTicketId) else "")),
    tree:    vars.treeResp.sha,
    parents: [vars.changePlan.headSha]
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <http:request method="POST"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/commits']"
                      target="commitResp"
                      doc:name="POST create commit"
                      doc:id="pfac0001-0001-0001-0001-000000000022"
                      doc:description="Create the upgrade commit object; response contains the commit SHA" />

        <set-variable variableName="commitSha"
                      value="#[vars.commitResp.sha]"
                      doc:name="PV SET commitSha"
                      doc:id="pfac0001-0001-0001-0001-000000000023" />

        <logger level="INFO"
                doc:name="LOG INFO commit created"
                doc:id="pfac0001-0001-0001-0001-000000000024"
                message="#['LOG INFO | flow=pf-atomic-commit | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | branchName=' ++ vars.branchName ++ ' | commitSha=' ++ (vars.commitSha default '?')]" />

        <!-- ── Step 7: Move branch ref to the new commit ──────────────────────────────────
             PATCH /repos/{owner}/{repo}/git/refs/heads/{branchName}
             Body: { sha: {commitSha}, force: false }
             ──────────────────────────────────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Build update-ref body"
                      doc:id="pfac0001-0001-0001-0001-000000000025">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    sha:   vars.commitSha,
    force: false
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <http:request method="PATCH"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/refs/heads/' ++ vars.branchName]"
                      doc:name="PATCH move branch ref to commit"
                      doc:id="pfac0001-0001-0001-0001-000000000026"
                      doc:description="Point the upgrade branch at the new commit SHA" />

        <!-- ── Step 8: Write branch index + persist branch/commit to job record ──────────
             branch index: key='branch::{branchName}' → jobId  (read by webhook handler)
             job record:   merge branchName + commitSha + updatedAt (status NOT changed)
             ──────────────────────────────────────────────────────────────────────────────── -->
        <os:store key="#['branch::' ++ vars.branchName]"
                  objectStore="jobStore"
                  doc:name="OS Store branch index (branch→jobId)"
                  doc:id="pfac0001-0001-0001-0001-000000000027"
                  doc:description="Index entry read by the webhook handler to map an incoming branch event back to a jobId">
            <os:value>#[vars.jobId]</os:value>
        </os:store>

        <os:retrieve key="#[vars.jobId]"
                     objectStore="jobStore"
                     doc:name="OS Retrieve job record for branch/commit update"
                     doc:id="pfac0001-0001-0001-0001-000000000028">
            <os:default-value>#[{}]</os:default-value>
        </os:retrieve>

        <ee:transform doc:name="DW Merge branchName + commitSha into job record"
                      doc:id="pfac0001-0001-0001-0001-000000000029"
                      doc:description="Persist branchName and commitSha; status is set by pf-start-upgrade via pf-set-status after this flow returns">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
payload ++ {
    branchName: vars.branchName,
    commitSha:  vars.commitSha,
    updatedAt:  now() as String {format: "yyyy-MM-dd'T'HH:mm:ss'Z'"}
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <os:store key="#[vars.jobId]"
                  objectStore="jobStore"
                  doc:name="OS Store job record with branch + commit"
                  doc:id="pfac0001-0001-0001-0001-000000000030">
            <os:value>#[payload]</os:value>
        </os:store>

        <logger level="INFO"
                doc:name="LOG INFO pf-atomic-commit done"
                doc:id="pfac0001-0001-0001-0001-000000000031"
                message="#['LOG INFO | flow=pf-atomic-commit | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | branchName=' ++ vars.branchName ++ ' | commitSha=' ++ (vars.commitSha default '?') ++ ' | stagedFiles=' ++ sizeOf(vars.stagedFiles default [])]" />

    </sub-flow>

    <!-- ==========================================================================
         pf-open-pr
         Opens a GitHub Pull Request for the already-committed upgrade branch.
         Inputs:  vars.coords {owner,repo,defaultBranch}
                  vars.changePlan {targetRuntime,targetJavaVersion}
                  vars.branchName, vars.commitSha, vars.appName, vars.jobId
                  vars.jiraTicketId (optional)
         Outputs: vars.prNumber, vars.prUrl persisted to job record in OS.
                  Status is NOT changed here — pf-start-upgrade sets PR_OPEN.
         ========================================================================== -->
    <sub-flow name="pf-open-pr"
              doc:id="pfop0001-0001-0001-0001-000000000001"
              doc:name="pf-open-pr"
              doc:description="Open a GitHub Pull Request for the upgrade branch and persist prUrl to the job record.">

        <logger level="INFO"
                doc:name="LOG INFO pf-open-pr enter"
                doc:id="pfop0001-0001-0001-0001-000000000002"
                message="#['LOG INFO | flow=pf-open-pr | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | branchName=' ++ (vars.branchName default '?')]" />

        <!-- ── Step 1: Build PR body ──────────────────────────────────────────────────── -->
        <ee:transform doc:name="DW Build create-PR body"
                      doc:id="pfop0001-0001-0001-0001-000000000003"
                      doc:description="Compose the GitHub PR payload: title, head branch, base branch, body, flags">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    title: ("Java 17 upgrade: " ++ vars.appName ++ " → " ++ vars.changePlan.targetRuntime ++ " / Java " ++ vars.changePlan.targetJavaVersion),
    head:  vars.branchName,
    base:  vars.coords.defaultBranch,
    body:  ("Automated Java 17 upgrade for **" ++ vars.appName ++ "**.\n\n"
           ++ "- Target runtime: " ++ vars.changePlan.targetRuntime ++ "\n"
           ++ "- Target Java: " ++ vars.changePlan.targetJavaVersion ++ "\n"
           ++ "- Commit: " ++ vars.commitSha ++ "\n"
           ++ "- Job: " ++ vars.jobId
           ++ (if (vars.jiraTicketId != null) ("\n- Jira: " ++ vars.jiraTicketId) else "")),
    maintainer_can_modify: true,
    draft: false
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <!-- ── Step 2: Create PR ──────────────────────────────────────────────────────── -->
        <http:request method="POST"
                      config-ref="GitHub_Request"
                      path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/pulls']"
                      target="prResp"
                      doc:name="POST create Pull Request"
                      doc:id="pfop0001-0001-0001-0001-000000000004"
                      doc:description="Create the GitHub PR; response contains number and html_url" />

        <!-- ── Step 3: Capture PR number and URL ──────────────────────────────────────── -->
        <set-variable variableName="prNumber"
                      value="#[vars.prResp.number]"
                      doc:name="PV SET prNumber"
                      doc:id="pfop0001-0001-0001-0001-000000000005" />

        <set-variable variableName="prUrl"
                      value="#[vars.prResp.html_url]"
                      doc:name="PV SET prUrl"
                      doc:id="pfop0001-0001-0001-0001-000000000006" />

        <!-- ── Step 4: Persist prNumber + prUrl to the job record ────────────────────────
             Status is NOT set here — pf-start-upgrade calls pf-set-status(PR_OPEN) after return.
             ──────────────────────────────────────────────────────────────────────────────── -->
        <os:retrieve key="#[vars.jobId]"
                     objectStore="jobStore"
                     doc:name="OS Retrieve job record for PR update"
                     doc:id="pfop0001-0001-0001-0001-000000000007">
            <os:default-value>#[{}]</os:default-value>
        </os:retrieve>

        <ee:transform doc:name="DW Merge prNumber + prUrl into job record"
                      doc:id="pfop0001-0001-0001-0001-000000000008"
                      doc:description="Persist prNumber and prUrl; status unchanged — caller sets PR_OPEN via pf-set-status">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
payload ++ {
    prNumber:  vars.prNumber,
    prUrl:     vars.prUrl,
    updatedAt: now() as String {format: "yyyy-MM-dd'T'HH:mm:ss'Z'"}
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <os:store key="#[vars.jobId]"
                  objectStore="jobStore"
                  doc:name="OS Store job record with PR details"
                  doc:id="pfop0001-0001-0001-0001-000000000009">
            <os:value>#[payload]</os:value>
        </os:store>

        <!-- ── Step 5: Log success ────────────────────────────────────────────────────── -->
        <logger level="INFO"
                doc:name="LOG INFO PR created"
                doc:id="pfop0001-0001-0001-0001-000000000010"
                message="#['LOG INFO | flow=pf-open-pr | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | prNumber=' ++ vars.prNumber ++ ' | prUrl=' ++ (vars.prUrl default '?')]" />

    </sub-flow>

    <!-- ==========================================================================
         pf-notify  (success — PR opened)
         Sends Slack message + optional Jira comment after the PR is opened.
         NON-FATAL: the whole body is wrapped in <try> so a notification outage
         never fails the upgrade pipeline. Errors are logged at WARN and swallowed.
         Inputs: vars.appName, vars.jobId, vars.prUrl, vars.prNumber,
                 vars.branchName, vars.jiraTicketId (optional)
         ========================================================================== -->
    <sub-flow name="pf-notify"
              doc:id="pfnt0001-0001-0001-0001-000000000001"
              doc:name="pf-notify"
              doc:description="Send Slack + optional Jira PR-opened notification. Non-fatal.">

        <try doc:name="TRY notification (non-fatal)"
             doc:id="pfnt0001-0001-0001-0001-000000000002">

            <!-- 1. Entry log -->
            <logger level="INFO"
                    doc:name="LOG INFO pf-notify enter"
                    doc:id="pfnt0001-0001-0001-0001-000000000003"
                    message="#['LOG INFO | flow=pf-notify | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | prUrl=' ++ (vars.prUrl default '(none)')]" />

            <!-- 2. Slack success message -->
            <ee:transform doc:name="DW Build Slack success message"
                          doc:id="pfnt0001-0001-0001-0001-000000000004">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    channel: p('slack.channel'),
    text: (":rocket: *Java 17 upgrade PR ready* — " ++ vars.appName
           ++ "\n" ++ (vars.prUrl default "(no url)")
           ++ "\nJob " ++ vars.jobId ++ " • status PR_OPEN")
}]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <set-variable variableName="slackPath"
                          value="#[p('secure::slack.webhookUrl') replace 'https://hooks.slack.com' with '']"
                          doc:name="PV SET slackPath"
                          doc:id="pfnt0001-0001-0001-0001-000000000005" />

            <http:request method="POST"
                          config-ref="Slack_Request"
                          path="#[vars.slackPath]"
                          doc:name="POST Slack success notification"
                          doc:id="pfnt0001-0001-0001-0001-000000000006"
                          doc:description="Send PR-opened message to the configured Slack channel" />

            <!-- 3. Jira comment — only when a ticket was supplied -->
            <choice doc:name="CHOICE jiraTicketId present?"
                    doc:id="pfnt0001-0001-0001-0001-000000000007"
                    doc:description="Post an ADF comment only when a Jira ticket ID is available">
                <when expression="#[vars.jiraTicketId != null]"
                      doc:name="jiraTicketId present"
                      doc:id="pfnt0001-0001-0001-0001-000000000008">

                    <ee:transform doc:name="DW Build Jira ADF comment (success)"
                                  doc:id="pfnt0001-0001-0001-0001-000000000009"
                                  doc:description="Atlassian Document Format comment body with PR link">
                        <ee:message>
                            <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    body: {
        "type": "doc",
        version: 1,
        content: [
            {
                "type": "paragraph",
                content: [
                    { "type": "text", text: ("Java 17 upgrade PR opened for " ++ vars.appName ++ " — status PR_OPEN. ") },
                    { "type": "text", text: (vars.prUrl default ""), marks: [ { "type": "link", attrs: { href: (vars.prUrl default "") } } ] }
                ]
            }
        ]
    }
}]]></ee:set-payload>
                        </ee:message>
                    </ee:transform>

                    <http:request method="POST"
                                  config-ref="Jira_Request"
                                  path="#['/rest/api/3/issue/' ++ vars.jiraTicketId ++ '/comment']"
                                  doc:name="POST Jira comment (success)"
                                  doc:id="pfnt0001-0001-0001-0001-000000000010"
                                  doc:description="Post ADF PR-opened comment to the Jira ticket" />

                </when>
                <otherwise doc:name="no Jira ticket — skip"
                           doc:id="pfnt0001-0001-0001-0001-000000000011">
                    <logger level="DEBUG"
                            doc:name="LOG DEBUG no Jira ticket"
                            doc:id="pfnt0001-0001-0001-0001-000000000012"
                            message="#['LOG DEBUG | flow=pf-notify | no Jira ticket — skipping Jira comment']" />
                </otherwise>
            </choice>

            <!-- Non-fatal error handler: log and swallow so a notification outage never kills the pipeline -->
            <error-handler>
                <on-error-continue type="ANY"
                                   doc:name="swallow notification error"
                                   doc:id="pfnt0001-0001-0001-0001-000000000013">
                    <logger level="WARN"
                            doc:name="LOG WARN notification failed"
                            doc:id="pfnt0001-0001-0001-0001-000000000014"
                            message="#['LOG WARN | flow=pf-notify | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | notification error swallowed: ' ++ (error.description default 'unknown')]" />
                </on-error-continue>
            </error-handler>
        </try>

    </sub-flow>

    <!-- ==========================================================================
         pf-notify-failure  (failure — upgrade pipeline errored)
         Reads the authoritative status + error from the job record in Object Store
         (the error-handler in pf-start-upgrade has already written them there).
         Sends a Slack failure message. NON-FATAL: errors are logged at WARN and swallowed.
         Inputs: vars.appName, vars.jobId
         ========================================================================== -->
    <sub-flow name="pf-notify-failure"
              doc:id="pfnf0001-0001-0001-0001-000000000001"
              doc:name="pf-notify-failure"
              doc:description="Send Slack failure notification. Reads status/error from job record. Non-fatal.">

        <try doc:name="TRY failure notification (non-fatal)"
             doc:id="pfnf0001-0001-0001-0001-000000000002">

            <!-- 1. Entry log -->
            <logger level="WARN"
                    doc:name="LOG WARN pf-notify-failure enter"
                    doc:id="pfnf0001-0001-0001-0001-000000000003"
                    message="#['LOG WARN | flow=pf-notify-failure | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | sending failure notification']" />

            <!-- 2. Read authoritative failure data from the job record -->
            <os:retrieve key="#[vars.jobId]"
                         objectStore="jobStore"
                         doc:name="OS Retrieve job record (for failure notification)"
                         doc:id="pfnf0001-0001-0001-0001-000000000004">
                <os:default-value>#[{}]</os:default-value>
            </os:retrieve>

            <!-- Capture record fields before payload is overwritten by the Slack transform -->
            <set-variable variableName="failStatus"
                          value="#[payload.status default 'FAILED']"
                          doc:name="PV SET failStatus"
                          doc:id="pfnf0001-0001-0001-0001-000000000005" />
            <set-variable variableName="failError"
                          value="#[payload.error default 'unknown']"
                          doc:name="PV SET failError"
                          doc:id="pfnf0001-0001-0001-0001-000000000006" />

            <!-- 3. Slack failure message -->
            <ee:transform doc:name="DW Build Slack failure message"
                          doc:id="pfnf0001-0001-0001-0001-000000000007">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    channel: p('slack.channel'),
    text: (":x: *Java 17 upgrade FAILED* — " ++ vars.appName
           ++ "\nJob " ++ vars.jobId
           ++ " • status " ++ vars.failStatus
           ++ "\nError: " ++ vars.failError)
}]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <set-variable variableName="slackPath"
                          value="#[p('secure::slack.webhookUrl') replace 'https://hooks.slack.com' with '']"
                          doc:name="PV SET slackPath"
                          doc:id="pfnf0001-0001-0001-0001-000000000008" />

            <http:request method="POST"
                          config-ref="Slack_Request"
                          path="#[vars.slackPath]"
                          doc:name="POST Slack failure notification"
                          doc:id="pfnf0001-0001-0001-0001-000000000009"
                          doc:description="Send upgrade-failed message to the configured Slack channel" />

            <!-- Non-fatal error handler: log and swallow -->
            <error-handler>
                <on-error-continue type="ANY"
                                   doc:name="swallow failure notification error"
                                   doc:id="pfnf0001-0001-0001-0001-000000000010">
                    <logger level="WARN"
                            doc:name="LOG WARN failure notification itself failed"
                            doc:id="pfnf0001-0001-0001-0001-000000000011"
                            message="#['LOG WARN | flow=pf-notify-failure | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | failure notification error swallowed: ' ++ (error.description default 'unknown')]" />
                </on-error-continue>
            </error-handler>
        </try>

    </sub-flow>

    <!-- ==========================================================================
         pf-rollback
         Opens a revert PR that restores the repo to its pre-upgrade state.
         Called best-effort from mf-impl-post-cd-result on CD failure.
         NON-FATAL: the entire revert sequence is wrapped in <try>/<on-error-continue>.
         ALWAYS restores the job record to payload on return (required by the caller).
         Inputs: vars.coords {owner,repo,defaultBranch}, vars.branchName, vars.commitSha,
                 vars.jobId, vars.appName, vars.prNumber
         ========================================================================== -->
    <sub-flow name="pf-rollback"
              doc:id="pfrl0001-0001-0001-0001-000000000001"
              doc:name="pf-rollback"
              doc:description="Open a revert PR restoring the repo to pre-upgrade state. Non-fatal.">

        <!-- Revert sequence — any failure is swallowed so the caller's FAILED_DEPLOY write still runs -->
        <try doc:name="TRY revert PR (non-fatal)"
             doc:id="pfrl0001-0001-0001-0001-000000000003">

            <!-- Step 1: Entry log -->
            <logger level="INFO"
                    doc:name="LOG INFO pf-rollback enter"
                    doc:id="pfrl0001-0001-0001-0001-000000000004"
                    message="#['LOG INFO | flow=pf-rollback | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | branchName=' ++ (vars.branchName default '?')]" />

            <!-- Step 2: GET upgrade commit → find pre-upgrade base SHA (parents[0]) -->
            <http:request method="GET"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/commits/' ++ vars.commitSha]"
                          target="upgradeCommit"
                          doc:name="GET upgrade commit (find pre-upgrade base)"
                          doc:id="pfrl0001-0001-0001-0001-000000000005"
                          doc:description="Fetch the upgrade commit to extract its first parent SHA (=changePlan.headSha)" />

            <set-variable variableName="baseSha"
                          value="#[vars.upgradeCommit.parents[0].sha]"
                          doc:name="PV SET baseSha (pre-upgrade HEAD)"
                          doc:id="pfrl0001-0001-0001-0001-000000000006" />

            <!-- Step 3: GET base commit → extract its tree SHA (the pre-upgrade file tree) -->
            <http:request method="GET"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/commits/' ++ vars.baseSha]"
                          target="baseCommit"
                          doc:name="GET base commit (extract pre-upgrade tree SHA)"
                          doc:id="pfrl0001-0001-0001-0001-000000000007"
                          doc:description="Fetch the pre-upgrade commit object to get its tree SHA" />

            <set-variable variableName="baseTreeSha"
                          value="#[vars.baseCommit.tree.sha]"
                          doc:name="PV SET baseTreeSha"
                          doc:id="pfrl0001-0001-0001-0001-000000000008" />

            <!-- Step 4: GET current default-branch HEAD -->
            <http:request method="GET"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/commits/' ++ vars.coords.defaultBranch]"
                          target="defHead"
                          doc:name="GET current default-branch HEAD"
                          doc:id="pfrl0001-0001-0001-0001-000000000009"
                          doc:description="Fetch the current HEAD of the default branch to anchor the revert branch" />

            <set-variable variableName="defHeadSha"
                          value="#[vars.defHead.sha]"
                          doc:name="PV SET defHeadSha"
                          doc:id="pfrl0001-0001-0001-0001-000000000010" />

            <!-- Step 5: Compute revert branch name -->
            <set-variable variableName="revertBranch"
                          value="#['revert/' ++ vars.branchName]"
                          doc:name="PV SET revertBranch"
                          doc:id="pfrl0001-0001-0001-0001-000000000011" />

            <!-- Step 6: Create the revert branch ref at current default HEAD -->
            <ee:transform doc:name="DW Build create revert-branch ref body"
                          doc:id="pfrl0001-0001-0001-0001-000000000012">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ ref: ("refs/heads/" ++ vars.revertBranch), sha: vars.defHeadSha }]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <http:request method="POST"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/refs']"
                          doc:name="POST create revert branch ref"
                          doc:id="pfrl0001-0001-0001-0001-000000000013"
                          doc:description="Create the revert branch anchored at the current default-branch HEAD" />

            <!-- Step 7: Create revert commit (tree=pre-upgrade base tree, parent=current default HEAD) -->
            <ee:transform doc:name="DW Build revert commit body"
                          doc:id="pfrl0001-0001-0001-0001-000000000014">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    message: ("Revert Java 17 upgrade (" ++ vars.jobId ++ ") — deploy failed"),
    tree:    vars.baseTreeSha,
    parents: [vars.defHeadSha]
}]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <http:request method="POST"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/commits']"
                          target="revertCommit"
                          doc:name="POST create revert commit"
                          doc:id="pfrl0001-0001-0001-0001-000000000015"
                          doc:description="Commit that restores the repo to the pre-upgrade tree" />

            <!-- Step 8: Move the revert branch ref to the new revert commit -->
            <ee:transform doc:name="DW Build update revert-branch ref body"
                          doc:id="pfrl0001-0001-0001-0001-000000000016">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ sha: vars.revertCommit.sha, force: false }]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <http:request method="PATCH"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/git/refs/heads/' ++ vars.revertBranch]"
                          doc:name="PATCH move revert branch to revert commit"
                          doc:id="pfrl0001-0001-0001-0001-000000000017"
                          doc:description="Point the revert branch at the new revert commit SHA" />

            <!-- Step 9: Open the revert PR -->
            <ee:transform doc:name="DW Build revert PR body"
                          doc:id="pfrl0001-0001-0001-0001-000000000018">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    title:                ("Revert: Java 17 upgrade " ++ vars.appName),
    head:                 vars.revertBranch,
    base:                 vars.coords.defaultBranch,
    body:                 ("Automated rollback for job " ++ vars.jobId ++ " — the CD deploy failed."),
    maintainer_can_modify: true
}]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <http:request method="POST"
                          config-ref="GitHub_Request"
                          path="#['/repos/' ++ vars.coords.owner ++ '/' ++ vars.coords.repo ++ '/pulls']"
                          target="revertPr"
                          doc:name="POST open revert PR"
                          doc:id="pfrl0001-0001-0001-0001-000000000019"
                          doc:description="Open the revert PR; response contains html_url" />

            <set-variable variableName="revertPrUrl"
                          value="#[vars.revertPr.html_url]"
                          doc:name="PV SET revertPrUrl"
                          doc:id="pfrl0001-0001-0001-0001-000000000020" />

            <!-- Step 10: Persist revertPrUrl onto the job record -->
            <os:retrieve key="#[vars.jobId]"
                         objectStore="jobStore"
                         doc:name="OS Retrieve job record for revertPrUrl update"
                         doc:id="pfrl0001-0001-0001-0001-000000000021">
                <os:default-value>#[{}]</os:default-value>
            </os:retrieve>

            <ee:transform doc:name="DW Merge revertPrUrl into job record"
                          doc:id="pfrl0001-0001-0001-0001-000000000022">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
payload ++ {
    revertPrUrl: vars.revertPrUrl,
    updatedAt:   now() as String {format: "yyyy-MM-dd'T'HH:mm:ss'Z'"}
}]]></ee:set-payload>
                </ee:message>
            </ee:transform>

            <os:store key="#[vars.jobId]"
                      objectStore="jobStore"
                      doc:name="OS Store job record with revertPrUrl"
                      doc:id="pfrl0001-0001-0001-0001-000000000023">
                <os:value>#[payload]</os:value>
            </os:store>

            <!-- Step 11: Log rollback PR opened -->
            <logger level="INFO"
                    doc:name="LOG INFO rollback PR opened"
                    doc:id="pfrl0001-0001-0001-0001-000000000024"
                    message="#['LOG INFO | flow=pf-rollback | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | revertPrUrl=' ++ (vars.revertPrUrl default '?')]" />

            <!-- Non-fatal error handler — swallow any failure so the caller's FAILED_DEPLOY merge still runs -->
            <error-handler>
                <on-error-continue type="ANY"
                                   doc:id="pfrl0001-0001-0001-0001-000000000025">
                    <logger level="WARN"
                            doc:name="LOG WARN pf-rollback failed"
                            doc:id="pfrl0001-0001-0001-0001-000000000026"
                            message="#['LOG WARN | flow=pf-rollback | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default '?') ++ ' | rollback error swallowed: ' ++ (error.description default 'unknown')]" />
                </on-error-continue>
            </error-handler>
        </try>

        <!--
            ALWAYS restore job record to payload after the try (success or failure).
            The caller (mf-impl-post-cd-result) does payload ++ { FAILED_DEPLOY fields }
            immediately after this sub-flow returns, so the job record MUST be in payload.
        -->
        <os:retrieve key="#[vars.jobId]"
                     objectStore="jobStore"
                     doc:name="OS Retrieve job record (restore to payload for caller)"
                     doc:id="pfrl0001-0001-0001-0001-000000000027">
            <os:default-value>#[{}]</os:default-value>
        </os:retrieve>

    </sub-flow>

    <!-- pf-get-job-status: retrieves a job record from Object Store         -->
    <!-- Requires: vars.jobId                                                -->
    <!-- Returns:  payload = JobStatus JSON (public fields only)             -->
    <!-- Throws:   OS:KEY_NOT_FOUND → mapped to 404 by error handler         -->
    <sub-flow name="pf-get-job-status"
              doc:id="pf000003-0003-0003-0003-000000000001"
              doc:name="pf-get-job-status"
              doc:description="Shared: retrieve job from Object Store by jobId. Called by REST and MCP.">

        <logger level="DEBUG"
                doc:name="LOG DEBUG Status Lookup"
                doc:id="pf000003-0003-0003-0003-000000000002"
                message="#['LOG DEBUG | flow=pf-get-job-status | correlationId=' ++ correlationId ++ ' | jobId=' ++ (vars.jobId default 'unknown')]" />

        <os:retrieve key="#[vars.jobId]"
                     objectStore="jobStore"
                     doc:name="OS Retrieve Job Record"
                     doc:id="pf000003-0003-0003-0003-000000000003" />

        <ee:transform doc:name="DW Build JobStatus Response"
                      doc:id="pf000003-0003-0003-0003-000000000004">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
// Human-readable message + recommended poll interval keyed by status.
// nextPollSeconds = 0 means terminal — the caller should stop polling.
var statusMeta = {
    PROCESSING:    { message: "Upgrade accepted and queued.",                               nextPollSeconds: 5  },
    ASSESSING:     { message: "Analyzing the repository and computing the change plan.",    nextPollSeconds: 5  },
    COMMITTING:    { message: "Applying transforms and committing changes to a branch.",    nextPollSeconds: 5  },
    COMMITTED:     { message: "Changes committed; opening a pull request.",                 nextPollSeconds: 5  },
    PR_OPEN:       { message: "Pull request is open and ready for review/merge.",           nextPollSeconds: 0  },
    DEPLOYING:     { message: "PR merged; CI/CD is building and deploying.",                nextPollSeconds: 10 },
    DEPLOYED:      { message: "Upgrade deployed successfully.",                             nextPollSeconds: 0  },
    FAILED_ASSESS: { message: "Assessment failed. See error for details.",                  nextPollSeconds: 0  },
    FAILED_COMMIT: { message: "Commit/transform stage failed. See error for details.",      nextPollSeconds: 0  },
    FAILED_CI:     { message: "CI build/tests failed after merge. See error for details.",  nextPollSeconds: 0  },
    FAILED_DEPLOY: { message: "Deployment failed. See error for details.",                  nextPollSeconds: 0  }
}
var meta = statusMeta[payload.status]
           default { message: ("Status: " ++ (payload.status default "UNKNOWN")), nextPollSeconds: 10 }
---
{
    jobId:           payload.jobId,
    status:          payload.status,
    message:         meta.message,
    nextPollSeconds: meta.nextPollSeconds
}
++ (if (payload.branchName  != null) { branchName:  payload.branchName  } else {})
++ (if (payload.prUrl       != null) { prUrl:        payload.prUrl       } else {})
++ (if (payload.prNumber    != null) { prNumber:     payload.prNumber    } else {})
++ (if (payload.completedAt != null) { completedAt:  payload.completedAt } else {})
++ (if (payload.error       != null) { error:        payload.error       } else {})]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <logger level="DEBUG"
                doc:name="LOG DEBUG Status Found"
                doc:id="pf000003-0003-0003-0003-000000000005"
                message="#['LOG DEBUG | flow=pf-get-job-status | correlationId=' ++ correlationId ++ ' | jobId=' ++ vars.jobId ++ ' | status=' ++ payload.status]" />

    </sub-flow>

</mule>
