<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd">

    <!--
        Implementation flow for POST /webhook.
        Receives GitHub pull_request webhook events.

        Security:   HMAC-SHA256 signature verified before any state change.
        Idempotency: delivery UUID de-duplicated via Object Store.

        Job status transitions triggered here:
          PR_OPEN â†’ DEPLOYING  (on successful merge â€” CI/CD outcome handled by /webhook/cd-result)
    -->
    <flow name="mf-impl-post-webhook" doc:id="b8c9d0e1-0008-0008-0008-000000000001">

        <!-- â”€â”€ HMAC signature validation â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
             Compute expected = "sha256=" ++ HMAC-SHA256(webhookSecret, rawBody).
             vars.rawBody and vars.ghSignature were captured in the main flow BEFORE APIkit
             parsed the payload, so rawBody is the exact bytes GitHub signed.
             â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
        <logger level="INFO" doc:name="LOG INFO START mf-impl-post-webhook" doc:id="fe86b326-6568-42f3-8884-59eef55cc130" message="#[output application/java --- 'START - mf-impl-post-webhook']" />
		<ee:transform doc:name="DW Compute Expected HMAC Signature"
                      doc:id="b8c9d0e1-0008-0008-0008-000000000020"
                      doc:description="Compute sha256=HEX(HMAC-SHA256(webhookSecret, rawBody)) for comparison with x-hub-signature-256">
            <ee:variables>
                <ee:set-variable variableName="expectedSig"><![CDATA[%dw 2.0
import dw::Crypto
output text/plain
---
"sha256=" ++ Crypto::HMACWith(p('secure::github.webhookSecret') as Binary, vars.rawBody, "HmacSHA256")]]></ee:set-variable>
            </ee:variables>
        </ee:transform>
		<choice doc:name="CHOICE signature valid?"
                doc:id="b8c9d0e1-0008-0008-0008-000000000022"
                doc:description="Reject any delivery whose x-hub-signature-256 header does not match the computed HMAC">
            <when expression="#[vars.ghSignature == '' or vars.ghSignature != vars.expectedSig]"
                  doc:name="signature missing or invalid"
                  doc:id="b8c9d0e1-0008-0008-0008-000000000023">

                <logger level="WARN"
                        doc:name="LOG WARN Invalid Webhook Signature"
                        doc:id="b8c9d0e1-0008-0008-0008-000000000024"
                        message="#[output application/java --- 'INVALID webhook signature | delivery=' ++ vars.ghDelivery ++ ' | event=' ++ vars.ghEvent]" />

                <set-variable variableName="httpStatus"
                              value="401"
                              doc:name="PV SET httpStatus 401"
                              doc:id="b8c9d0e1-0008-0008-0008-000000000025" />

                <ee:transform doc:name="DW Set 401 Error Body"
                              doc:id="b8c9d0e1-0008-0008-0008-000000000026">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ error: "invalid signature", correlationId: correlationId }]]></ee:set-payload>
                    </ee:message>
                </ee:transform>

            </when>
			<otherwise doc:name="signature valid â€” proceed"
                       doc:id="b8c9d0e1-0008-0008-0008-000000000027">

                <!-- â”€â”€ Idempotency: de-duplicate by GitHub delivery UUID â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
                     NOTE: os:retrieve treats a NULL os:default-value as "no default" and throws
                     OS:KEY_NOT_FOUND on a missing key. A brand-new delivery (the common case) has
                     no key, so we default to `false` (not seen) â€” this also preserves the payload,
                     which a null default + error handler would not. -->
                <os:retrieve key="#['delivery::' ++ vars.ghDelivery]"
                             objectStore="idempotencyStore"
                             target="seenDelivery"
                             doc:name="OS Retrieve delivery idempotency key"
                             doc:id="b8c9d0e1-0008-0008-0008-000000000028">
                    <os:default-value>#[false]</os:default-value>
                </os:retrieve>

                <choice doc:name="CHOICE duplicate delivery?"
                        doc:id="b8c9d0e1-0008-0008-0008-000000000029"
                        doc:description="Skip processing if this delivery UUID was already handled">
                    <when expression="#[vars.seenDelivery == true]"
                          doc:name="duplicate delivery â€” skip"
                          doc:id="b8c9d0e1-0008-0008-0008-000000000030">

                        <logger level="INFO"
                                doc:name="LOG INFO Duplicate Delivery Skipped"
                                doc:id="b8c9d0e1-0008-0008-0008-000000000031"
                                message="#[output application/java --- 'duplicate delivery, skipping | delivery=' ++ vars.ghDelivery]" />

                        <ee:transform doc:name="DW Set 200 Duplicate Acknowledgement"
                                      doc:id="b8c9d0e1-0008-0008-0008-000000000032">
                            <ee:message>
                                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, duplicate: true, correlationId: correlationId }]]></ee:set-payload>
                            </ee:message>
                        </ee:transform>

                    </when>
                    <otherwise doc:name="new delivery â€” process"
                               doc:id="b8c9d0e1-0008-0008-0008-000000000033">

                        <!-- Mark this delivery as processed before touching any job state -->
                        <os:store key="#['delivery::' ++ vars.ghDelivery]"
                                  objectStore="idempotencyStore"
                                  doc:name="OS Store delivery idempotency key"
                                  doc:id="b8c9d0e1-0008-0008-0008-000000000034">
                            <os:value>#[true]</os:value>
                        </os:store>

                        <logger level="DEBUG"
                                doc:name="LOG DEBUG Webhook Received"
                                doc:id="b8c9d0e1-0008-0008-0008-000000000002"
                                message="#[output application/java --- 'webhook received | event=' ++ vars.ghEvent ++ ' | action=' ++ (payload.action default 'unknown') ++ ' | delivery=' ++ vars.ghDelivery]" />

                        <!--
                            Only process pull_request events where the PR was merged.
                            All other events are acknowledged without state changes.
                        -->
                        <choice doc:name="Is PR Merge Event?"
                                doc:id="b8c9d0e1-0008-0008-0008-000000000003">

                            <when expression="#[payload.action == 'closed' and payload.pull_request.merged == true]"
                                  doc:name="PR merged">

                                <set-variable variableName="mergedBranch"
                                              value="#[(payload.pull_request.head.ref default '') as String]"
                                              doc:name="PV SET mergedBranch"
                                              doc:id="b8c9d0e1-0008-0008-0008-000000000004" />

                                <set-variable variableName="prUrl"
                                              value="#[(payload.pull_request.html_url default '') as String]"
                                              doc:name="PV SET prUrl"
                                              doc:id="b8c9d0e1-0008-0008-0008-000000000005" />

                                <logger level="INFO"
                                        doc:name="LOG INFO PR Merged"
                                        doc:id="b8c9d0e1-0008-0008-0008-000000000006"
                                        message="#[output application/java --- 'PR merged | branch=' ++ vars.mergedBranch ++ ' | prUrl=' ++ vars.prUrl]" />

                                <!-- get-or-empty: an untracked merged branch has no key. A null
                                     os:default-value would throw OS:KEY_NOT_FOUND, so default to ""
                                     (no mapping) â€” this also preserves the payload. -->
                                <os:retrieve key="#['branch::' ++ vars.mergedBranch]"
                                             objectStore="indexStore"
                                             target="jobIdFromBranch"
                                             doc:name="OS Retrieve jobId by Branch"
                                             doc:id="b8c9d0e1-0008-0008-0008-000000000007">
                                    <os:default-value>#[""]</os:default-value>
                                </os:retrieve>

                                <choice doc:name="Is Branch Mapped to a Job?"
                                        doc:id="b8c9d0e1-0008-0008-0008-000000000008">

                                    <when expression="#[vars.jobIdFromBranch != null and vars.jobIdFromBranch != '']"
                                          doc:name="branch mapped to job">

                                        <os:retrieve key="#[vars.jobIdFromBranch]"
                                                     objectStore="jobStore"
                                                     doc:name="OS Retrieve Job Record"
                                                     doc:id="b8c9d0e1-0008-0008-0008-000000000009" />

                                        <set-variable variableName="jobId" value="#[vars.jobIdFromBranch]"
                                                      doc:name="PV SET jobId" doc:id="b8c9d0e1-0008-0008-0008-0000000000a0" />
                                        <set-variable variableName="appName" value="#[payload.appName default '-']"
                                                      doc:name="PV SET appName" doc:id="b8c9d0e1-0008-0008-0008-0000000000a1" />
                                        <flow-ref name="pf-mdc-stamp" doc:name="pf-mdc-stamp" doc:id="b8c9d0e1-0008-0008-0008-0000000000a2" />

                                        <!--
                                            Transition to DEPLOYING â€” the final DEPLOYED/FAILED_DEPLOY
                                            transition is set by the CD pipeline via /webhook/cd-result.
                                            completedAt is NOT set here; it belongs to the terminal state.
                                        -->
                                        <ee:transform doc:name="DW Update Job Status to DEPLOYING"
                                                      doc:id="b8c9d0e1-0008-0008-0008-000000000010">
                                            <ee:message>
                                                <ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
payload mergeWith {
    status:     "DEPLOYING",
    prUrl:      vars.prUrl,
    branchName: vars.mergedBranch,
    updatedAt:  nowUtc()
}]]></ee:set-payload>
                                            </ee:message>
                                        </ee:transform>

                                        <os:store key="#[vars.jobIdFromBranch]"
                                                  objectStore="jobStore"
                                                  doc:name="OS Update Job to DEPLOYING"
                                                  doc:id="b8c9d0e1-0008-0008-0008-000000000011">
                                            <os:value>#[payload]</os:value>
                                        </os:store>

                                        <!-- Slack + Jira: PR merged, now deploying (non-fatal, payload-preserving) -->
                                        <set-variable variableName="jiraTicketId" value="#[payload.jiraTicketId default null]"
                                                      doc:name="PV SET jiraTicketId (deploying)" doc:id="b8c9d0e1-0008-0008-0008-000000000014" />
                                        <ee:transform doc:name="DW Build DEPLOYING notification vars" doc:id="b8c9d0e1-0008-0008-0008-000000000015">
                                            <ee:message /><ee:variables>
                                                <ee:set-variable variableName="eventSlackText"><![CDATA[%dw 2.0
output text/plain
---
":rocket: *PR merged - deploying* - " ++ (vars.appName default "unknown")
++ "\nJob " ++ (vars.jobId default "?") ++ " is DEPLOYING; CI/CD is building and deploying."
++ (if (vars.prUrl != null) ("\n" ++ (vars.prUrl as String)) else "")]]></ee:set-variable>
                                                <ee:set-variable variableName="jiraCommentText"><![CDATA[%dw 2.0
output text/plain
---
"The Java 17 upgrade PR for " ++ (vars.appName as String) ++ " (job " ++ (vars.jobId as String) ++ ") was merged. Deployment is in progress (DEPLOYING)."]]></ee:set-variable>
                                                <ee:set-variable variableName="jiraCommentUrl"><![CDATA[vars.prUrl default null]]></ee:set-variable>
                                            </ee:variables>
                                        </ee:transform>
                                        <flow-ref name="pf-notify-event" doc:name="pf-notify-event (deploying)"
                                                  doc:id="b8c9d0e1-0008-0008-0008-000000000016" />

                                        <logger level="INFO"
                                                doc:name="LOG INFO Job DEPLOYING"
                                                doc:id="b8c9d0e1-0008-0008-0008-000000000012"
                                                message="#[output application/java --- 'job DEPLOYING | awaiting CD pipeline callback']" />

                                    </when>

                                    <otherwise>
                                        <logger level="DEBUG"
                                                doc:name="LOG DEBUG Branch Not Tracked"
                                                doc:id="b8c9d0e1-0008-0008-0008-000000000013"
                                                message="#[output application/java --- 'branch not tracked, ignoring | branch=' ++ vars.mergedBranch]" />
                                    </otherwise>

                                </choice>

                            </when>

                            <!-- PR closed WITHOUT merge → self-heal: release the lock + park CLOSED,
                                 so a manually-abandoned upgrade PR never blocks future runs. -->
                            <when expression="#[payload.action == 'closed' and (payload.pull_request.merged default false) != true]"
                                  doc:name="PR closed (unmerged)">
                                <set-variable variableName="closedBranch"
                                              value="#[(payload.pull_request.head.ref default '') as String]"
                                              doc:name="PV SET closedBranch"
                                              doc:id="b8c9d0e1-0008-0008-0008-000000000060" />
                                <set-variable variableName="prUrl"
                                              value="#[(payload.pull_request.html_url default '') as String]"
                                              doc:name="PV SET prUrl"
                                              doc:id="b8c9d0e1-0008-0008-0008-000000000061" />
                                <logger level="INFO"
                                        doc:name="LOG INFO PR closed unmerged"
                                        doc:id="b8c9d0e1-0008-0008-0008-000000000062"
                                        message="#[output application/java --- 'PR closed without merge | branch=' ++ vars.closedBranch ++ ' | prUrl=' ++ vars.prUrl]" />
                                <flow-ref name="pf-handle-pr-abandoned"
                                          doc:name="pf-handle-pr-abandoned"
                                          doc:id="b8c9d0e1-0008-0008-0008-000000000063" />
                            </when>

                            <otherwise>
                                <logger level="DEBUG"
                                        doc:name="LOG DEBUG Non-Merge Event Ignored"
                                        doc:id="b8c9d0e1-0008-0008-0008-000000000014"
                                        message="#[output application/java --- 'webhook event ignored | event=' ++ vars.ghEvent ++ ' | action=' ++ (payload.action default 'unknown')]" />
                            </otherwise>

                        </choice>

                        <!-- Return 200 to GitHub for all valid, non-duplicate events -->
                        <ee:transform doc:name="DW Set 200 Acknowledgement"
                                      doc:id="b8c9d0e1-0008-0008-0008-000000000015">
                            <ee:message>
                                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, correlationId: correlationId }]]></ee:set-payload>
                            </ee:message>
                        </ee:transform>

                    </otherwise>
                </choice>

            </otherwise>
        </choice>
		<logger level="INFO" doc:name="LOG INFO END mf-impl-post-webhook" doc:id="0f156594-23b9-47af-a9f6-dee19f900c13" message="#[output application/java --- 'END - mf-impl-post-webhook']" />

    </flow>

    <!--
        mf-impl-post-cd-result â€” POST /webhook/cd-result
        Called by the GitHub Actions CD pipeline after deployment.
        Body: { jobId: String, result: "success"|"failure", deployUrl?: String }
        Auth: HMAC-SHA256 over the raw body in header X-CD-Signature-256 ("sha256=" ++ hex),
              same scheme as POST /webhook. Transitional fallback: X-CD-Token == secure::github.webhookSecret
              (kept so existing local tests / un-migrated CD pipelines keep working; remove once the CD
              job signs — see DESIGN_DECISIONS #1).
        Finalizes the job: DEPLOYED (success) or FAILED_DEPLOY (failure).
    -->
    <flow name="mf-impl-post-cd-result"
          doc:id="b8c9d0e1-0008-0008-0008-000000000016"
          doc:name="mf-impl-post-cd-result"
          doc:description="Receive CD pipeline result callback; authenticate, finalize job status, release lock.">

        <!-- â”€â”€ Step 1: Shared-token authentication â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
        <!-- Compute expected HMAC over the raw body (identical scheme to POST /webhook).
             vars.rawBody is captured in the main flow for every route. -->
        <logger level="INFO" doc:name="LOG INFO START mf-impl-post-cd-result" doc:id="d60c9e42-e3d3-4b9a-b001-406cd8b7c364" message="#[output application/java --- 'START - mf-impl-post-cd-result']" />
		<ee:transform doc:name="DW Compute Expected CD HMAC Signature"
                      doc:id="b9c0d1e2-0009-0009-0009-0000000000a1"
                      doc:description="Compute sha256=HEX(HMAC-SHA256(webhookSecret, rawBody)) for comparison with X-CD-Signature-256">
            <ee:variables>
                <ee:set-variable variableName="expectedCdSig"><![CDATA[%dw 2.0
import dw::Crypto
output text/plain
---
// Null-safe: if rawBody is absent (e.g. an MUnit or caller that did not pass through
// the main flow), skip HMAC and return "" so the signature branch is a no-match and the
// X-CD-Token fallback authenticates. Real callbacks always carry rawBody.
if (vars.rawBody != null)
    "sha256=" ++ Crypto::HMACWith(p('secure::github.webhookSecret') as Binary, vars.rawBody as Binary, "HmacSHA256")
else ""]]></ee:set-variable>
            </ee:variables>
        </ee:transform>
        <set-variable variableName="cdSignature"
                      value="#[attributes.headers['x-cd-signature-256'] default '']"
                      doc:name="PV SET cdSignature"
                      doc:id="b9c0d1e2-0009-0009-0009-0000000000a2" />
        <set-variable variableName="cdToken"
                      value="#[attributes.headers['x-cd-token'] default '']"
                      doc:name="PV SET cdToken"
                      doc:id="b9c0d1e2-0009-0009-0009-0000000000a3" />

        <choice doc:name="CHOICE cd-result auth valid?"
                doc:id="b9c0d1e2-0009-0009-0009-000000000001"
                doc:description="Reject unless a valid HMAC signature (X-CD-Signature-256) OR the transitional shared token (X-CD-Token) is presented">
            <when expression="#[not ( (vars.cdSignature != '' and vars.cdSignature == vars.expectedCdSig) or (vars.cdToken != '' and vars.cdToken == p('secure::github.webhookSecret')) )]"
                  doc:name="auth missing or invalid"
                  doc:id="b9c0d1e2-0009-0009-0009-000000000002">

                <logger level="WARN"
                        doc:name="LOG WARN Invalid CD Auth"
                        doc:id="b9c0d1e2-0009-0009-0009-000000000003"
                        message="#[output application/java --- 'invalid CD auth (X-CD-Signature-256/X-CD-Token) â€” rejecting']" />

                <set-variable variableName="httpStatus"
                              value="401"
                              doc:name="PV SET httpStatus 401"
                              doc:id="b9c0d1e2-0009-0009-0009-000000000004" />

                <ee:transform doc:name="DW Set 401 Error Body"
                              doc:id="b9c0d1e2-0009-0009-0009-000000000005">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ error: "invalid cd token", correlationId: correlationId }]]></ee:set-payload>
                    </ee:message>
                </ee:transform>

            </when>
            <otherwise doc:name="token valid â€” proceed"
                       doc:id="b9c0d1e2-0009-0009-0009-000000000006">

                <!-- â”€â”€ Step 2: Parse CD callback body â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->

                <!-- â”€â”€ Step 3: Load job record (throws OS:KEY_NOT_FOUND â†’ 404 if missing) â”€â”€ -->
                <ee:transform doc:name="PV SET payload vars" doc:id="63c02425-9a3f-4309-ad9b-850f577a592d">
					<ee:message>
					</ee:message>
					<ee:variables >
						<ee:set-variable variableName="jobId" ><![CDATA[payload.jobId]]></ee:set-variable>
						<ee:set-variable variableName="cdResult" ><![CDATA[payload.result default '']]></ee:set-variable>
						<ee:set-variable variableName="stage" ><![CDATA[payload.stage default 'deploy']]></ee:set-variable>
						<ee:set-variable variableName="deployUrl" ><![CDATA[payload.deployUrl default null]]></ee:set-variable>
						<ee:set-variable variableName="cdError" ><![CDATA[payload.error default null]]></ee:set-variable>
						<!-- dependency-guard: summarized violations array (captured here, before os:retrieve
						     overwrites payload with the job record). Empty for other stages. -->
						<ee:set-variable variableName="cdReport" ><![CDATA[payload.report default []]]></ee:set-variable>
					</ee:variables>
				</ee:transform>
				<os:retrieve key="#[vars.jobId]"
                             objectStore="jobStore"
                             doc:name="OS Retrieve Job Record"
                             doc:id="b9c0d1e2-0009-0009-0009-000000000011" />

                <!-- â”€â”€ Step 4: Capture record fields before any transform overwrites payload â”€
                     jobRec holds the full record; downstream sub-flows (verify/rollback) run
                     HTTP calls that clobber `payload`, so finalize transforms build from jobRec. -->
				<ee:transform doc:name="PVT SET record vars" doc:id="665264f0-76f0-4ddb-8e2d-4b2478119611">
											<ee:message>
											</ee:message>
					<ee:variables>
						<ee:set-variable variableName="jobRec"><![CDATA[payload]]></ee:set-variable>
						<ee:set-variable variableName="appName"><![CDATA[payload.appName]]></ee:set-variable>
						<ee:set-variable variableName="coords"><![CDATA[payload.coords]]></ee:set-variable>
						<ee:set-variable variableName="branchName"><![CDATA[payload.branchName]]></ee:set-variable>
						<ee:set-variable variableName="commitSha"><![CDATA[payload.commitSha]]></ee:set-variable>
						<ee:set-variable variableName="prNumber"><![CDATA[payload.prNumber]]></ee:set-variable>
						<ee:set-variable variableName="curStatus"><![CDATA[payload.status default '']]></ee:set-variable>
						<ee:set-variable variableName="environment" ><![CDATA[payload.environment default '']]></ee:set-variable>
						<ee:set-variable variableName="jiraTicketId" ><![CDATA[payload.jiraTicketId default null]]></ee:set-variable>
						<ee:set-variable variableName="prUrl" ><![CDATA[payload.prUrl default null]]></ee:set-variable>
					</ee:variables>
										</ee:transform>

				<flow-ref name="pf-mdc-stamp" doc:name="pf-mdc-stamp" doc:id="b9c0d1e2-0009-0009-0009-0000000000aa" />

                <!-- â”€â”€ Step 5: Idempotency / terminal guard â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
                     If the job is already in a terminal state, return 200 and skip re-processing.
                     â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
				<!-- ── Step 5: Stage discriminator (test | dependency-guard | deploy) ─────────────
				     ONE endpoint serves three CI/CD stages, keyed off payload.stage (default "deploy"):
				       · stage=test             → MUnit CI result. failure parks the job in MUNIT_FAILED
				                                  (Slack + Jira, retained 30d); success resumes a parked job.
				       · stage=dependency-guard → Java-17 dependency gate result (full tree, incl. transitives).
				                                  failure parks the job in DEP_GUARD_FAILED with a summarized
				                                  violations report (Slack + Jira, retained 30d); success resumes.
				       · stage=deploy           → deployment result — the existing finalize logic below.
				     ──────────────────────────────────────────────────────────────────────────── -->
				<choice doc:name="CHOICE cd stage?"
				        doc:id="c1d2e3f4-000a-000a-000a-000000000001"
				        doc:description="Route MUnit (test) and dependency-guard callbacks to their park/resume handlers; deploy callbacks to finalize">

				    <when expression="#[vars.stage == 'test']"
				          doc:name="stage=test (MUnit CI result)"
				          doc:id="c1d2e3f4-000a-000a-000a-000000000002">

				        <choice doc:name="CHOICE munit result?"
				                doc:id="c1d2e3f4-000a-000a-000a-000000000003"
				                doc:description="MUnit failure → park MUNIT_FAILED; success → resume if parked">

				            <!-- ── test FAILURE → park in MUNIT_FAILED (idempotent) ─────────────── -->
				            <when expression="#[vars.cdResult == 'failure']"
				                  doc:name="munit failure"
				                  doc:id="c1d2e3f4-000a-000a-000a-000000000004">
				                <choice doc:name="CHOICE parkable?"
				                        doc:id="c1d2e3f4-000a-000a-000a-000000000005"
				                        doc:description="Ignore when already parked or already in a terminal state">
				                    <when expression="#[['MUNIT_FAILED','DEPLOYED','FAILED_DEPLOY','FAILED_CI','FAILED_ASSESS','FAILED_COMMIT','FAILED_INTERRUPTED'] contains (vars.curStatus default '')]"
				                          doc:name="already parked/terminal — idempotent"
				                          doc:id="c1d2e3f4-000a-000a-000a-000000000006">
				                        <logger level="INFO" doc:name="LOG INFO MUnit failure idempotent"
				                                doc:id="c1d2e3f4-000a-000a-000a-000000000007"
				                                message="#[output application/java --- 'munit failure ignored (already ' ++ (vars.curStatus default '?') ++ ')']" />
				                        <ee:transform doc:name="DW 200 MUnit idempotent" doc:id="c1d2e3f4-000a-000a-000a-000000000008">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, idempotent: true, jobId: vars.jobId, status: vars.curStatus }]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                    </when>
				                    <otherwise doc:name="park in MUNIT_FAILED"
				                               doc:id="c1d2e3f4-000a-000a-000a-000000000009">
				                        <!-- Persist MUNIT_FAILED. jobStore TTL (osv2.jobTtlSeconds=30d) is refreshed
				                             on store, so the parked job is retained ~30 days. The single-flight lock
				                             is intentionally NOT released (the app is still mid-upgrade). -->
				                        <ee:transform doc:name="DW Update Job to MUNIT_FAILED" doc:id="c1d2e3f4-000a-000a-000a-00000000000a">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
vars.jobRec mergeWith {
    status:    "MUNIT_FAILED",
    error:     (vars.cdError default "MUnit tests failed in CI"),
    updatedAt: nowUtc()
}]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                        <os:store key="#[vars.jobId]" objectStore="jobStore"
				                                  doc:name="OS Store MUNIT_FAILED" doc:id="c1d2e3f4-000a-000a-000a-00000000000b">
				                            <os:value>#[payload]</os:value>
				                        </os:store>

				                        <!-- Slack notification — non-fatal -->
				                        <try doc:name="TRY Slack MUNIT_FAILED (non-fatal)" doc:id="c1d2e3f4-000a-000a-000a-00000000000c">
				                            <ee:transform doc:name="DW Build Slack MUnit-failed message" doc:id="c1d2e3f4-000a-000a-000a-00000000000d">
				                                <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    channel: p('slack.channel'),
    text: (":x: *MUnit tests FAILED* — " ++ vars.appName
           ++ "\nJob " ++ vars.jobId ++ " is paused (MUNIT_FAILED)."
           ++ "\nFix the tests; the upgrade resumes automatically when CI reports a test success."
           ++ (if (vars.jiraTicketId != null) ("\nJira: <" ++ (p('jira.baseUrl') default '') ++ "/browse/" ++ (vars.jiraTicketId as String) ++ "|" ++ (vars.jiraTicketId as String) ++ ">") else "")
           ++ (if (vars.prUrl != null) ("\n" ++ (vars.prUrl as String)) else ""))
}]]></ee:set-payload></ee:message>
				                            </ee:transform>
				                            <set-variable variableName="slackPath"
				                                          value="#[p('secure::slack.webhookUrl') replace 'https://hooks.slack.com' with '']"
				                                          doc:name="PV SET slackPath" doc:id="c1d2e3f4-000a-000a-000a-00000000000e" />
				                            <http:request method="POST" config-ref="Slack_Request" path="#[vars.slackPath]"
				                                          doc:name="POST Slack MUNIT_FAILED" doc:id="c1d2e3f4-000a-000a-000a-00000000000f" />
				                            <error-handler>
				                                <on-error-continue type="ANY" doc:id="c1d2e3f4-000a-000a-000a-000000000010">
				                                    <logger level="WARN" doc:name="LOG WARN Slack MUnit-failed failed"
				                                            doc:id="c1d2e3f4-000a-000a-000a-000000000011"
				                                            message="#[output application/java --- 'Slack MUNIT_FAILED notification error swallowed: ' ++ (error.description default 'unknown')]" />
				                                </on-error-continue>
				                            </error-handler>
				                        </try>

				                        <!-- Jira comment — non-fatal (pf-jira-comment skips when no ticket) -->
				                        <set-variable variableName="jiraCommentText"
				                                      value="#[output text/plain --- ('MUnit tests FAILED in CI for ' ++ (vars.appName as String) ++ ' (job ' ++ (vars.jobId as String) ++ '). The upgrade is paused (MUNIT_FAILED); fix the tests and CI will resume it on the next successful test run. ' ++ ((vars.cdError default '') as String))]"
				                                      doc:name="PV SET jiraCommentText (munit failed)" doc:id="c1d2e3f4-000a-000a-000a-000000000012" />
				                        <set-variable variableName="jiraCommentUrl" value="#[vars.prUrl default null]"
				                                      doc:name="PV SET jiraCommentUrl (munit failed)" doc:id="c1d2e3f4-000a-000a-000a-000000000013" />
				                        <flow-ref name="pf-jira-comment" doc:name="pf-jira-comment (munit failed)"
				                                  doc:id="c1d2e3f4-000a-000a-000a-000000000014" />

				                        <logger level="WARN" doc:name="LOG WARN Job MUNIT_FAILED"
				                                doc:id="c1d2e3f4-000a-000a-000a-000000000015"
				                                message="#[output application/java --- 'job MUNIT_FAILED | awaiting MUnit fix']" />

				                        <ee:transform doc:name="DW 200 MUNIT_FAILED Ack" doc:id="c1d2e3f4-000a-000a-000a-000000000016">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, jobId: vars.jobId, status: "MUNIT_FAILED" }]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                    </otherwise>
				                </choice>
				            </when>

				            <!-- ── test SUCCESS → resume a parked job (else no-op) ──────────────── -->
				            <when expression="#[vars.cdResult == 'success']"
				                  doc:name="munit success"
				                  doc:id="c1d2e3f4-000a-000a-000a-000000000017">
				                <choice doc:name="CHOICE was parked?"
				                        doc:id="c1d2e3f4-000a-000a-000a-000000000018"
				                        doc:description="Resume only when the job is currently parked in MUNIT_FAILED">
				                    <when expression="#[vars.curStatus == 'MUNIT_FAILED']"
				                          doc:name="parked — resume to PR_OPEN"
				                          doc:id="c1d2e3f4-000a-000a-000a-000000000019">
				                        <ee:transform doc:name="DW Resume Job to PR_OPEN" doc:id="c1d2e3f4-000a-000a-000a-00000000001a">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
vars.jobRec mergeWith {
    status:    "PR_OPEN",
    error:     null,
    updatedAt: nowUtc()
}]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                        <os:store key="#[vars.jobId]" objectStore="jobStore"
				                                  doc:name="OS Store resumed PR_OPEN" doc:id="c1d2e3f4-000a-000a-000a-00000000001b">
				                            <os:value>#[payload]</os:value>
				                        </os:store>

				                        <try doc:name="TRY Slack MUnit-resumed (non-fatal)" doc:id="c1d2e3f4-000a-000a-000a-00000000001c">
				                            <ee:transform doc:name="DW Build Slack MUnit-resumed message" doc:id="c1d2e3f4-000a-000a-000a-00000000001d">
				                                <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    channel: p('slack.channel'),
    text: (":white_check_mark: *MUnit tests passing again* — " ++ vars.appName
           ++ "\nJob " ++ vars.jobId ++ " resumed (PR_OPEN); ready to merge/deploy."
           ++ (if (vars.prUrl != null) ("\n" ++ (vars.prUrl as String)) else ""))
}]]></ee:set-payload></ee:message>
				                            </ee:transform>
				                            <set-variable variableName="slackPath"
				                                          value="#[p('secure::slack.webhookUrl') replace 'https://hooks.slack.com' with '']"
				                                          doc:name="PV SET slackPath (resume)" doc:id="c1d2e3f4-000a-000a-000a-00000000001e" />
				                            <http:request method="POST" config-ref="Slack_Request" path="#[vars.slackPath]"
				                                          doc:name="POST Slack MUnit-resumed" doc:id="c1d2e3f4-000a-000a-000a-00000000001f" />
				                            <error-handler>
				                                <on-error-continue type="ANY" doc:id="c1d2e3f4-000a-000a-000a-000000000020">
				                                    <logger level="WARN" doc:name="LOG WARN Slack MUnit-resumed failed"
				                                            doc:id="c1d2e3f4-000a-000a-000a-000000000021"
				                                            message="#[output application/java --- 'Slack MUnit-resumed notification error swallowed: ' ++ (error.description default 'unknown')]" />
				                                </on-error-continue>
				                            </error-handler>
				                        </try>

				                        <set-variable variableName="jiraCommentText"
				                                      value="#[output text/plain --- ('MUnit tests are passing again for ' ++ (vars.appName as String) ++ ' (job ' ++ (vars.jobId as String) ++ '). The upgrade has resumed (PR_OPEN) and is ready to merge/deploy.')]"
				                                      doc:name="PV SET jiraCommentText (resumed)" doc:id="c1d2e3f4-000a-000a-000a-000000000022" />
										<set-variable variableName="jiraCommentUrl" value="#[vars.prUrl default null]"
				                                      doc:name="PV SET jiraCommentUrl (resumed)" doc:id="c1d2e3f4-000a-000a-000a-000000000023" />
				                        <flow-ref name="pf-jira-comment" doc:name="pf-jira-comment (resumed)"
				                                  doc:id="c1d2e3f4-000a-000a-000a-000000000024" />

				                        <logger level="INFO" doc:name="LOG INFO Job resumed"
				                                doc:id="c1d2e3f4-000a-000a-000a-000000000025"
				                                message="#[output application/java --- 'job resumed | MUNIT_FAILED→PR_OPEN (MUnit green)']" />

				                        <ee:transform doc:name="DW 200 resumed Ack" doc:id="c1d2e3f4-000a-000a-000a-000000000026">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, resumed: true, jobId: vars.jobId, status: "PR_OPEN" }]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                    </when>
				                    <otherwise doc:name="not parked"
				                               doc:id="c1d2e3f4-000a-000a-000a-000000000027">
				                        <choice doc:name="CHOICE PR_OPEN — record MUnit passed?"
				                                doc:id="c1d2e3f4-000b-000b-000b-000000000001"
				                                doc:description="A test success while PR_OPEN is a distinct sub-stage (MUnit passed): mark it + notify. Other statuses stay note-only.">
				                            <when expression="#[(vars.curStatus default '') == 'PR_OPEN']"
				                                  doc:name="PR_OPEN — mark MUnit passed + notify"
				                                  doc:id="c1d2e3f4-000b-000b-000b-000000000002">
				                                <!-- Persist a MUnit-passed marker so GET /jobs/{jobId} can surface the
				                                     sub-stage via `message` (status stays PR_OPEN — enum unchanged). -->
				                                <ee:transform doc:name="DW Mark MUnit passed" doc:id="c1d2e3f4-000b-000b-000b-000000000003">
				                                    <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
vars.jobRec mergeWith {
    munit:     { result: "passed", at: nowUtc() },
    updatedAt: nowUtc()
}]]></ee:set-payload></ee:message>
				                                </ee:transform>
				                                <os:store key="#[vars.jobId]" objectStore="jobStore"
				                                          doc:name="OS Store MUnit-passed marker" doc:id="c1d2e3f4-000b-000b-000b-000000000004">
				                                    <os:value>#[payload]</os:value>
				                                </os:store>

				                                <!-- Slack + Jira: MUnit passed (non-fatal, payload-preserving) -->
				                                <ee:transform doc:name="DW Build MUnit-passed notification vars" doc:id="c1d2e3f4-000b-000b-000b-000000000005">
				                                    <ee:message /><ee:variables>
				                                        <ee:set-variable variableName="eventSlackText"><![CDATA[%dw 2.0
output text/plain
---
":white_check_mark: *MUnit tests passed* - " ++ (vars.appName default "unknown")
++ "\nJob " ++ (vars.jobId default "?") ++ " is PR_OPEN and ready for review/merge."
++ (if (vars.prUrl != null) ("\n" ++ (vars.prUrl as String)) else "")]]></ee:set-variable>
				                                        <ee:set-variable variableName="jiraCommentText"><![CDATA[%dw 2.0
output text/plain
---
"MUnit tests passed in CI for " ++ (vars.appName as String) ++ " (job " ++ (vars.jobId as String) ++ "). The pull request is open and ready for review/merge."]]></ee:set-variable>
				                                        <ee:set-variable variableName="jiraCommentUrl"><![CDATA[vars.prUrl default null]]></ee:set-variable>
				                                    </ee:variables>
				                                </ee:transform>
				                                <flow-ref name="pf-notify-event" doc:name="pf-notify-event (MUnit passed)"
				                                          doc:id="c1d2e3f4-000b-000b-000b-000000000006" />

				                                <logger level="INFO" doc:name="LOG INFO MUnit passed (PR_OPEN)"
				                                        doc:id="c1d2e3f4-000b-000b-000b-000000000007"
				                                        message="#[output application/java --- 'munit passed | PR_OPEN marker set + notified | job=' ++ (vars.jobId default '?')]" />
				                                <ee:transform doc:name="DW 200 MUnit passed Ack" doc:id="c1d2e3f4-000b-000b-000b-000000000008">
				                                    <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, jobId: vars.jobId, status: "PR_OPEN", munit: "passed" }]]></ee:set-payload></ee:message>
				                                </ee:transform>
				                            </when>
				                            <otherwise doc:name="note only"
				                                       doc:id="c1d2e3f4-000b-000b-000b-000000000009">
				                                <logger level="INFO" doc:name="LOG INFO MUnit success noted"
				                                        doc:id="c1d2e3f4-000a-000a-000a-000000000028"
				                                message="#[output application/java --- 'munit success noted (job not parked, status=' ++ (vars.curStatus default '?') ++ ')']" />
				                                <ee:transform doc:name="DW 200 MUnit success noted" doc:id="c1d2e3f4-000a-000a-000a-000000000029">
				                                    <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, jobId: vars.jobId, status: vars.curStatus, note: "munit success noted" }]]></ee:set-payload></ee:message>
				                                </ee:transform>
				                            </otherwise>
				                        </choice>
				                    </otherwise>
				                </choice>
				            </when>

				            <!-- ── unknown test result → 400 ───────────────────────────────────── -->
				            <otherwise doc:name="unknown munit result — reject"
				                       doc:id="c1d2e3f4-000a-000a-000a-00000000002a">
				                <logger level="WARN" doc:name="LOG WARN Unknown MUnit result"
				                        doc:id="c1d2e3f4-000a-000a-000a-00000000002b"
				                        message="#[output application/java --- 'unknown stage=test result=' ++ vars.cdResult]" />
				                <set-variable variableName="httpStatus" value="400"
				                              doc:name="PV SET httpStatus 400" doc:id="c1d2e3f4-000a-000a-000a-00000000002c" />
				                <ee:transform doc:name="DW 400 Unknown MUnit Result" doc:id="c1d2e3f4-000a-000a-000a-00000000002d">
				                    <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ error: "invalid result (expected success|failure)", correlationId: correlationId }]]></ee:set-payload></ee:message>
				                </ee:transform>
				            </otherwise>

				        </choice>

				    </when>

				    <!-- ── stage=dependency-guard (Java-17 dependency gate, incl. transitives) ────────
				         The CI dependency guard resolves the FULL Maven tree and validates every Mule
				         extension against the Java-17 floors BEFORE build/deploy:
				           · failure → park the job in DEP_GUARD_FAILED (retained 30d, resumable), store
				                       the summarized violations report, Slack + Jira notify.
				           · success → resume a job parked in DEP_GUARD_FAILED back to PR_OPEN.
				         Mirrors the stage=test park/resume contract; the lock is NOT released on failure
				         (the app is still mid-upgrade). ───────────────────────────────────────────────── -->
				    <when expression="#[vars.stage == 'dependency-guard']"
				          doc:name="stage=dependency-guard (Java 17 dep gate)"
				          doc:id="c1d2e3f4-000c-000c-000c-000000000001">

				        <choice doc:name="CHOICE dep-guard result?"
				                doc:id="c1d2e3f4-000c-000c-000c-000000000002"
				                doc:description="Guard failure → park DEP_GUARD_FAILED; success → resume if parked">

				            <!-- ── dep-guard FAILURE → park in DEP_GUARD_FAILED (idempotent) ───────── -->
				            <when expression="#[vars.cdResult == 'failure']"
				                  doc:name="dep-guard failure"
				                  doc:id="c1d2e3f4-000c-000c-000c-000000000003">
				                <choice doc:name="CHOICE parkable?"
				                        doc:id="c1d2e3f4-000c-000c-000c-000000000004"
				                        doc:description="Ignore when already parked or already in a terminal state">
				                    <when expression="#[['DEP_GUARD_FAILED','MUNIT_FAILED','DEPLOYED','FAILED_DEPLOY','FAILED_CI','FAILED_ASSESS','FAILED_COMMIT','FAILED_INTERRUPTED'] contains (vars.curStatus default '')]"
				                          doc:name="already parked/terminal — idempotent"
				                          doc:id="c1d2e3f4-000c-000c-000c-000000000005">
				                        <logger level="INFO" doc:name="LOG INFO dep-guard failure idempotent"
				                                doc:id="c1d2e3f4-000c-000c-000c-000000000006"
				                                message="#[output application/java --- 'dep-guard failure ignored (already ' ++ (vars.curStatus default '?') ++ ')']" />
				                        <ee:transform doc:name="DW 200 dep-guard idempotent" doc:id="c1d2e3f4-000c-000c-000c-000000000007">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, idempotent: true, jobId: vars.jobId, status: vars.curStatus }]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                    </when>
				                    <otherwise doc:name="park in DEP_GUARD_FAILED"
				                               doc:id="c1d2e3f4-000c-000c-000c-000000000008">
				                        <!-- Persist DEP_GUARD_FAILED + the summarized violations report. jobStore TTL
				                             (osv2.jobTtlSeconds=30d) is refreshed on store, so the parked job is retained
				                             ~30 days. The single-flight lock is intentionally NOT released. -->
				                        <ee:transform doc:name="DW Update Job to DEP_GUARD_FAILED" doc:id="c1d2e3f4-000c-000c-000c-000000000009">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
vars.jobRec mergeWith {
    status:    "DEP_GUARD_FAILED",
    error:     (vars.cdError default "Java 17 dependency violations found in CI"),
    depGuard:  { report: (vars.cdReport default []), at: nowUtc() },
    updatedAt: nowUtc()
}]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                        <os:store key="#[vars.jobId]" objectStore="jobStore"
				                                  doc:name="OS Store DEP_GUARD_FAILED" doc:id="c1d2e3f4-000c-000c-000c-00000000000a">
				                            <os:value>#[payload]</os:value>
				                        </os:store>

				                        <!-- Slack + Jira (non-fatal, payload-preserving via pf-notify-event) -->
				                        <ee:transform doc:name="DW Build DEP_GUARD_FAILED notification vars" doc:id="c1d2e3f4-000c-000c-000c-00000000000b">
				                            <ee:message /><ee:variables>
				                                <ee:set-variable variableName="eventSlackText"><![CDATA[%dw 2.0
output text/plain
var rpt = (vars.cdReport default [])
var lines = rpt map ((v) -> "• " ++ ((v.groupId default "?") as String) ++ ":" ++ ((v.artifactId default "?") as String)
                            ++ " resolved " ++ ((v.resolvedVersion default "?") as String)
                            ++ " < required " ++ ((v.requiredVersion default "?") as String))
---
":no_entry: *Java 17 dependency guard FAILED* - " ++ (vars.appName default "unknown")
++ "\nJob " ++ (vars.jobId default "?") ++ " is paused (DEP_GUARD_FAILED)."
++ (if (!isEmpty(lines)) ("\nJava 17-incompatible dependencies were resolved (incl. transitives):\n" ++ (lines joinBy "\n")) else "")
++ "\nFix the connector pins/versions; the upgrade resumes automatically when CI reports a dependency-guard success."
++ (if (vars.jiraTicketId != null) ("\nJira: <" ++ (p('jira.baseUrl') default '') ++ "/browse/" ++ (vars.jiraTicketId as String) ++ "|" ++ (vars.jiraTicketId as String) ++ ">") else "")
++ (if (vars.prUrl != null) ("\n" ++ (vars.prUrl as String)) else "")]]></ee:set-variable>
				                                <ee:set-variable variableName="jiraCommentText"><![CDATA[%dw 2.0
output text/plain
var rpt = (vars.cdReport default [])
var lines = rpt map ((v) -> ((v.groupId default "?") as String) ++ ":" ++ ((v.artifactId default "?") as String)
                            ++ " (" ++ ((v.resolvedVersion default "?") as String) ++ " < " ++ ((v.requiredVersion default "?") as String) ++ ")")
---
"Java 17 dependency guard FAILED in CI for " ++ (vars.appName as String) ++ " (job " ++ (vars.jobId as String)
++ "). The upgrade is paused (DEP_GUARD_FAILED)."
++ (if (!isEmpty(lines)) (" Incompatible dependencies: " ++ (lines joinBy "; ") ++ ".") else "")
++ " Fix the connector pins/versions; CI resumes the job on the next successful dependency-guard run."]]></ee:set-variable>
				                                <ee:set-variable variableName="jiraCommentUrl"><![CDATA[vars.prUrl default null]]></ee:set-variable>
				                            </ee:variables>
				                        </ee:transform>
				                        <flow-ref name="pf-notify-event" doc:name="pf-notify-event (dep-guard failed)"
				                                  doc:id="c1d2e3f4-000c-000c-000c-00000000000c" />

				                        <logger level="WARN" doc:name="LOG WARN Job DEP_GUARD_FAILED"
				                                doc:id="c1d2e3f4-000c-000c-000c-00000000000d"
				                                message="#[output application/java --- 'job DEP_GUARD_FAILED | ' ++ (sizeOf(vars.cdReport default []) as String) ++ ' violation(s) | awaiting dependency fix']" />

				                        <ee:transform doc:name="DW 200 DEP_GUARD_FAILED Ack" doc:id="c1d2e3f4-000c-000c-000c-00000000000e">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, jobId: vars.jobId, status: "DEP_GUARD_FAILED", violations: sizeOf(vars.cdReport default []) }]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                    </otherwise>
				                </choice>
				            </when>

				            <!-- ── dep-guard SUCCESS → resume a parked job (else no-op) ───────────── -->
				            <when expression="#[vars.cdResult == 'success']"
				                  doc:name="dep-guard success"
				                  doc:id="c1d2e3f4-000c-000c-000c-00000000000f">
				                <choice doc:name="CHOICE was parked (dep-guard)?"
				                        doc:id="c1d2e3f4-000c-000c-000c-000000000010"
				                        doc:description="Resume only when the job is currently parked in DEP_GUARD_FAILED">
				                    <when expression="#[vars.curStatus == 'DEP_GUARD_FAILED']"
				                          doc:name="parked — resume to PR_OPEN"
				                          doc:id="c1d2e3f4-000c-000c-000c-000000000011">
				                        <ee:transform doc:name="DW Resume Job to PR_OPEN (dep-guard)" doc:id="c1d2e3f4-000c-000c-000c-000000000012">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
vars.jobRec mergeWith {
    status:    "PR_OPEN",
    error:     null,
    depGuard:  { result: "passed", at: nowUtc() },
    updatedAt: nowUtc()
}]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                        <os:store key="#[vars.jobId]" objectStore="jobStore"
				                                  doc:name="OS Store resumed PR_OPEN (dep-guard)" doc:id="c1d2e3f4-000c-000c-000c-000000000013">
				                            <os:value>#[payload]</os:value>
				                        </os:store>

				                        <ee:transform doc:name="DW Build dep-guard-resumed notification vars" doc:id="c1d2e3f4-000c-000c-000c-000000000014">
				                            <ee:message /><ee:variables>
				                                <ee:set-variable variableName="eventSlackText"><![CDATA[%dw 2.0
output text/plain
---
":white_check_mark: *Java 17 dependency guard passing again* - " ++ (vars.appName default "unknown")
++ "\nJob " ++ (vars.jobId default "?") ++ " resumed (PR_OPEN); ready to merge/deploy."
++ (if (vars.prUrl != null) ("\n" ++ (vars.prUrl as String)) else "")]]></ee:set-variable>
				                                <ee:set-variable variableName="jiraCommentText"><![CDATA[%dw 2.0
output text/plain
---
"The Java 17 dependency guard is passing again for " ++ (vars.appName as String) ++ " (job " ++ (vars.jobId as String)
++ "). The upgrade has resumed (PR_OPEN) and is ready to merge/deploy."]]></ee:set-variable>
				                                <ee:set-variable variableName="jiraCommentUrl"><![CDATA[vars.prUrl default null]]></ee:set-variable>
				                            </ee:variables>
				                        </ee:transform>
				                        <flow-ref name="pf-notify-event" doc:name="pf-notify-event (dep-guard resumed)"
				                                  doc:id="c1d2e3f4-000c-000c-000c-000000000015" />

				                        <logger level="INFO" doc:name="LOG INFO Job resumed (dep-guard)"
				                                doc:id="c1d2e3f4-000c-000c-000c-000000000016"
				                                message="#[output application/java --- 'job resumed | DEP_GUARD_FAILED->PR_OPEN (dependency guard green)']" />

				                        <ee:transform doc:name="DW 200 resumed Ack (dep-guard)" doc:id="c1d2e3f4-000c-000c-000c-000000000017">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, resumed: true, jobId: vars.jobId, status: "PR_OPEN" }]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                    </when>
				                    <otherwise doc:name="not parked (dep-guard) — note only"
				                               doc:id="c1d2e3f4-000c-000c-000c-000000000018">
				                        <logger level="INFO" doc:name="LOG INFO dep-guard success noted"
				                                doc:id="c1d2e3f4-000c-000c-000c-000000000019"
				                                message="#[output application/java --- 'dep-guard success noted (job not parked, status=' ++ (vars.curStatus default '?') ++ ')']" />
				                        <ee:transform doc:name="DW 200 dep-guard success noted" doc:id="c1d2e3f4-000c-000c-000c-00000000001a">
				                            <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, jobId: vars.jobId, status: vars.curStatus, note: "dependency-guard success noted" }]]></ee:set-payload></ee:message>
				                        </ee:transform>
				                    </otherwise>
				                </choice>
				            </when>

				            <!-- ── unknown dep-guard result → 400 ────────────────────────────────── -->
				            <otherwise doc:name="unknown dep-guard result — reject"
				                       doc:id="c1d2e3f4-000c-000c-000c-00000000001b">
				                <logger level="WARN" doc:name="LOG WARN Unknown dep-guard result"
				                        doc:id="c1d2e3f4-000c-000c-000c-00000000001c"
				                        message="#[output application/java --- 'unknown stage=dependency-guard result=' ++ vars.cdResult]" />
				                <set-variable variableName="httpStatus" value="400"
				                              doc:name="PV SET httpStatus 400" doc:id="c1d2e3f4-000c-000c-000c-00000000001d" />
				                <ee:transform doc:name="DW 400 Unknown dep-guard Result" doc:id="c1d2e3f4-000c-000c-000c-00000000001e">
				                    <ee:message><ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ error: "invalid result (expected success|failure)", correlationId: correlationId }]]></ee:set-payload></ee:message>
				                </ee:transform>
				            </otherwise>

				        </choice>

				    </when>

				    <otherwise doc:name="stage=deploy (finalize)"
				               doc:id="c1d2e3f4-000a-000a-000a-00000000002e">

				<!-- ── Step 5: Idempotency / terminal guard ──────────────────────────────────
				     If the job is already in a terminal state, return 200 and skip re-processing. -->
				<choice doc:name="CHOICE already terminal?"
                        doc:id="b9c0d1e2-0009-0009-0009-000000000018"
                        doc:description="Skip if the job was already finalized by a previous cd-result callback">
                    <when expression="#[vars.curStatus == 'DEPLOYED' or vars.curStatus == 'FAILED_DEPLOY']"
                          doc:name="job already terminal"
                          doc:id="b9c0d1e2-0009-0009-0009-000000000019">

                        <logger level="INFO"
                                doc:name="LOG INFO CD Result Already Final"
                                doc:id="b9c0d1e2-0009-0009-0009-000000000020"
                                message="#[output application/java --- 'cd-result already final, skipping | status=' ++ vars.curStatus]" />

                        <ee:transform doc:name="DW Set 200 Idempotent Acknowledgement"
                                      doc:id="b9c0d1e2-0009-0009-0009-000000000021">
                            <ee:message>
                                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, idempotent: true, jobId: vars.jobId, status: vars.curStatus }]]></ee:set-payload>
                            </ee:message>
                        </ee:transform>

                    </when>
                    <otherwise doc:name="job not yet terminal â€” proceed"
                               doc:id="b9c0d1e2-0009-0009-0009-000000000022">

                        <!-- â”€â”€ Step 6: Outcome branch â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
                        <choice doc:name="CHOICE cdResult success|failure|unknown?"
                                doc:id="b9c0d1e2-0009-0009-0009-000000000023"
                                doc:description="Finalize job as DEPLOYED (success) or FAILED_DEPLOY (failure)">

                            <!-- â”€â”€ 6a: SUCCESS (CI-reported) â€” confirm on platform before finalizing â”€ -->
                            <when expression="#[vars.cdResult == 'success']"
                                  doc:name="success"
                                  doc:id="b9c0d1e2-0009-0009-0009-000000000024">

                                <!-- â”€â”€ 6a-i: Post-deploy platform confirmation (ADR-015) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
                                     CI said success. Make ONE Application Manager call to confirm the
                                     app is actually healthy on CH2.0:
                                       - platform verified + healthy  â†’ DEPLOYED
                                       - platform verified + unhealthy â†’ FAILED_DEPLOY (discrepancy)
                                       - platform unreachable/disabled â†’ trust CI signal â†’ DEPLOYED
                                     â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
                                <choice doc:name="CHOICE verify enabled?"
                                        doc:id="b9c0d1e2-0009-0009-0009-00000000006a">
                                    <when expression="#[(p('anypoint.verify.enabled') default 'true') == 'true']"
                                          doc:name="verify enabled"
                                          doc:id="b9c0d1e2-0009-0009-0009-00000000006b">
                                        <ee:transform doc:name="PV SET vars" doc:id="74c02f41-5bd4-4f0e-9d40-5657c83652d0">
											<ee:message>
											</ee:message>
											<ee:variables>
												<ee:set-variable variableName="vfyEnv"><![CDATA[output application/java --- (vars.environment default '') as String]]></ee:set-variable>
												<ee:set-variable variableName="vfyOrgId"><![CDATA[output application/java --- (vars.coords.orgId default p('anypoint.defaultOrgId')) as String]]></ee:set-variable>
												<ee:set-variable variableName="vfyApp"><![CDATA[output application/java --- (vars.appName default '') as String]]></ee:set-variable>
											</ee:variables>
										</ee:transform>
                                        <flow-ref name="pf-verify-deployment"
                                                  doc:name="pf-verify-deployment"
                                                  doc:id="b9c0d1e2-0009-0009-0009-00000000006f" />
                                    </when>
                                    <otherwise doc:name="verify disabled"
                                               doc:id="b9c0d1e2-0009-0009-0009-000000000070">
                                        <ee:transform doc:name="DW SET vars.verify (disabled)"
                                                      doc:id="b9c0d1e2-0009-0009-0009-000000000071">
                                            <ee:variables>
                                                <ee:set-variable variableName="verify"><![CDATA[%dw 2.0
output application/java
---
{ verified: false, found: false, status: 'DISABLED', healthy: false, runtimeVersion: null }]]></ee:set-variable>
                                            </ee:variables>
                                        </ee:transform>
                                    </otherwise>
                                </choice>

                                <set-variable variableName="deployVerdict"
                                              value="#[if ((vars.verify.verified default false) and not (vars.verify.healthy default false)) 'FAILED_DEPLOY' else 'DEPLOYED']"
                                              doc:name="PV SET vars.deployVerdict"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000072" />

                                <ee:transform doc:name="DW Update Job Status (verified)"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000025">
                                    <ee:message>
                                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
vars.jobRec mergeWith {
    status:                 vars.deployVerdict,
    deployUrl:              vars.deployUrl,
    platformVerified:       (vars.verify.verified default false),
    platformStatus:         (vars.verify.status default null),
    platformRuntimeVersion: (vars.verify.runtimeVersion default null),
    completedAt:            nowUtc(),
    updatedAt:              nowUtc()
}
mergeWith (if (vars.deployVerdict == "FAILED_DEPLOY")
      { error: "CD reported success but platform status=" ++ (vars.verify.status default "UNKNOWN") }
    else {})]]></ee:set-payload>
                                    </ee:message>
                                </ee:transform>
								<os:store key="#[vars.jobId]"
                                          objectStore="jobStore"
                                          doc:name="OS Store finalized Job Record"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000026">
                                    <os:value>#[payload]</os:value>
                                </os:store>

                                <!-- Release single-flight lock â€” idempotent + non-fatal -->
                                <set-variable variableName="lockApp" value="#[vars.appName default '']"
                                              doc:name="PV lockApp" doc:id="b9c0d1e2-0009-0009-0009-000000000027" />
                                <flow-ref name="pf-release-lock" doc:name="pf-release-lock (finalized)"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000028" />

                                <!-- â”€â”€ 6a-ii: Notify by verdict â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
                                <choice doc:name="CHOICE notify by verdict?"
                                        doc:id="b9c0d1e2-0009-0009-0009-000000000073">
                                    <when expression="#[vars.deployVerdict == 'DEPLOYED']"
                                          doc:name="DEPLOYED â€” Slack success"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000074">
                                        <!-- Slack success notification â€” non-fatal -->
                                        <try doc:name="TRY Slack DEPLOYED notification (non-fatal)"
                                             doc:id="b9c0d1e2-0009-0009-0009-000000000031">
                                            <ee:transform doc:name="DW Build Slack DEPLOYED message"
                                                          doc:id="b9c0d1e2-0009-0009-0009-000000000032">
                                                <ee:message>
                                                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    channel: p('slack.channel'),
    text: (":white_check_mark: *Java 17 upgrade DEPLOYED* â€” " ++ vars.appName
           ++ "\nJob " ++ vars.jobId
           ++ (if ((vars.verify.verified default false)) ("\nPlatform status: " ++ (vars.verify.status default "UNKNOWN")) else "\nPlatform: unverified (trusting CI)")
           ++ (if (vars.jiraTicketId != null) ("\nJira: <" ++ (p('jira.baseUrl') default '') ++ "/browse/" ++ (vars.jiraTicketId as String) ++ "|" ++ (vars.jiraTicketId as String) ++ ">") else "")
           ++ (if (vars.deployUrl != null) ("\n" ++ vars.deployUrl) else ""))
}]]></ee:set-payload>
                                                </ee:message>
                                            </ee:transform>
                                            <set-variable variableName="slackPath"
                                                          value="#[p('secure::slack.webhookUrl') replace 'https://hooks.slack.com' with '']"
                                                          doc:name="PV SET slackPath"
                                                          doc:id="b9c0d1e2-0009-0009-0009-000000000033" />
                                            <http:request method="POST"
                                                          config-ref="Slack_Request"
                                                          path="#[vars.slackPath]"
                                                          doc:name="POST Slack DEPLOYED notification"
                                                          doc:id="b9c0d1e2-0009-0009-0009-000000000034" />
                                            <error-handler>
                                                <on-error-continue type="ANY"
                                                                   doc:id="b9c0d1e2-0009-0009-0009-000000000035">
                                                    <logger level="WARN"
                                                            doc:name="LOG WARN Slack DEPLOYED notification failed"
                                                            doc:id="b9c0d1e2-0009-0009-0009-000000000036"
                                                            message="#[output application/java --- 'Slack DEPLOYED notification error swallowed: ' ++ (error.description default 'unknown')]" />
                                                </on-error-continue>
                                            </error-handler>
                                        </try>
                                    </when>
                                    <otherwise doc:name="FAILED_DEPLOY â€” discrepancy notify"
                                               doc:id="b9c0d1e2-0009-0009-0009-000000000075">
                                        <flow-ref name="pf-notify-failure"
                                                  doc:name="pf-notify-failure (platform discrepancy)"
                                                  doc:id="b9c0d1e2-0009-0009-0009-000000000076" />
                                    </otherwise>
                                </choice>

                                <!-- â”€â”€ 6a-iii: Jira comment on the final decision (with PR link) â”€â”€â”€â”€ -->
<set-variable variableName="jiraCommentText"
              value="#[output text/plain
---
if (vars.deployVerdict == 'DEPLOYED')
    ('Java 17 upgrade DEPLOYED for ' ++ (vars.appName as String) ++ ' (job ' ++ (vars.jobId as String) ++ '). Platform status: ' ++ ((vars.verify.status default 'n/a') as String) ++ '.')
else
    ('Java 17 upgrade marked FAILED_DEPLOY for ' ++ (vars.appName as String) ++ ' (job ' ++ (vars.jobId as String) ++ '). CI reported success but platform status=' ++ ((vars.verify.status default 'UNKNOWN') as String) ++ '.')]"
                                              doc:name="PV SET vars.jiraCommentText (finalize)"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000037a" />
                                <set-variable variableName="jiraCommentUrl"
                                              value="#[vars.prUrl default vars.deployUrl default null]"
                                              doc:name="PV SET vars.jiraCommentUrl (finalize)"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000037b" />
                                <flow-ref name="pf-jira-comment"
                                          doc:name="pf-jira-comment (final decision)"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000037c" />

                                <logger level="INFO"
                                        doc:name="LOG INFO Job finalized"
                                        doc:id="b9c0d1e2-0009-0009-0009-000000000037"
                                        message="#[output application/java --- 'job finalized | status=' ++ vars.deployVerdict ++ ' | platformVerified=' ++ (vars.verify.verified default false) ++ ' | platformStatus=' ++ (vars.verify.status default 'n/a') ++ ' | deployUrl=' ++ (vars.deployUrl default 'n/a')]" />

                                <ee:transform doc:name="DW Set 200 finalized Acknowledgement"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000038">
                                    <ee:message>
                                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, jobId: vars.jobId, status: vars.deployVerdict, platformVerified: (vars.verify.verified default false) }]]></ee:set-payload>
                                    </ee:message>
                                </ee:transform>

                            </when>

                            <!-- â”€â”€ 6b: FAILURE â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
                            <when expression="#[vars.cdResult == 'failure']"
                                  doc:name="failure"
                                  doc:id="b9c0d1e2-0009-0009-0009-000000000039">

                                <flow-ref name="pf-rollback"
                                          doc:name="pf-rollback (best-effort)"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000040" />

                                <ee:transform doc:name="DW Update Job Status to FAILED_DEPLOY"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000041">
                                    <ee:message>
                                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
import mergeWith from dw::core::Objects
import nowUtc from dwl::dates
---
vars.jobRec mergeWith {
    status:      "FAILED_DEPLOY",
    error:       (vars.cdError default "CD pipeline reported failure"),
    rolledBack:  true,
    completedAt: nowUtc(),
    updatedAt:   nowUtc()
}]]></ee:set-payload>
                                    </ee:message>
                                </ee:transform>

                                <os:store key="#[vars.jobId]"
                                          objectStore="jobStore"
                                          doc:name="OS Store FAILED_DEPLOY Job Record"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000042">
                                    <os:value>#[payload]</os:value>
                                </os:store>

                                <!-- Release single-flight lock â€” idempotent + non-fatal -->
                                <set-variable variableName="lockApp" value="#[vars.appName default '']"
                                              doc:name="PV lockApp" doc:id="b9c0d1e2-0009-0009-0009-000000000043" />
                                <flow-ref name="pf-release-lock" doc:name="pf-release-lock (failed deploy)"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000044" />

                                <flow-ref name="pf-notify-failure"
                                          doc:name="pf-notify-failure (FAILED_DEPLOY)"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000047" />

                                <!-- Jira comment on the final decision (link to the ROLLBACK PR when one was opened) -->
                                <set-variable variableName="jiraCommentText"
                                              value="#[output text/plain --- ('Java 17 upgrade FAILED_DEPLOY for ' ++ (vars.appName as String) ++ ' (job ' ++ (vars.jobId as String) ++ '). ' ++ ((vars.cdError default 'CD pipeline reported failure') as String) ++ (if ((vars.revertPrUrl default '') != '') ' A rollback (revert) PR has been opened to restore the pre-upgrade state.' else ' Rollback attempted.'))]"
                                              doc:name="PV SET vars.jiraCommentText (failure)"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000047a" />
                                <set-variable variableName="jiraCommentUrl"
                                              value="#[vars.revertPrUrl default vars.prUrl default null]"
                                              doc:name="PV SET vars.jiraCommentUrl (failure)"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000047b" />
                                <flow-ref name="pf-jira-comment"
                                          doc:name="pf-jira-comment (failure)"
                                          doc:id="b9c0d1e2-0009-0009-0009-000000000047c" />

                                <logger level="WARN"
                                        doc:name="LOG WARN Job FAILED_DEPLOY"
                                        doc:id="b9c0d1e2-0009-0009-0009-000000000048"
                                        message="#[output application/java --- 'job FAILED_DEPLOY']" />

                                <ee:transform doc:name="DW Set 200 FAILED_DEPLOY Acknowledgement"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000049">
                                    <ee:message>
                                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ acknowledged: true, jobId: vars.jobId, status: "FAILED_DEPLOY" }]]></ee:set-payload>
                                    </ee:message>
                                </ee:transform>

                            </when>

                            <!-- â”€â”€ 6c: UNKNOWN result â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ -->
                            <otherwise doc:name="unknown result â€” reject"
                                       doc:id="b9c0d1e2-0009-0009-0009-000000000050">

                                <logger level="WARN"
                                        doc:name="LOG WARN Unknown CD Result"
                                        doc:id="b9c0d1e2-0009-0009-0009-000000000051"
                                        message="#[output application/java --- 'unknown cdResult=' ++ vars.cdResult]" />

                                <set-variable variableName="httpStatus"
                                              value="400"
                                              doc:name="PV SET httpStatus 400"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000052" />

                                <ee:transform doc:name="DW Set 400 Unknown Result Body"
                                              doc:id="b9c0d1e2-0009-0009-0009-000000000053">
                                    <ee:message>
                                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ error: "invalid result (expected success|failure)", correlationId: correlationId }]]></ee:set-payload>
                                    </ee:message>
                                </ee:transform>

                            </otherwise>

                        </choice>

                    </otherwise>
                </choice>

				    </otherwise>
				</choice>

            </otherwise>
        </choice>
		<logger level="INFO" doc:name="LOG INFO END mf-impl-post-cd-result" doc:id="70fcd4ed-91a1-42dc-a1f5-4fe90e0af87f" message="#[output application/java --- 'END - mf-impl-post-cd-result']" />

    </flow>

</mule>
